# WebSocket server (Tier 4 sync)

Placeholder for the Yjs / Hocuspocus WebSocket service (P2+). Vercel hosts `@app/web`; this service runs on **Railway** or **Fly.io** for long-lived connections.

## P0

No implementation yet. See `railway.toml` and `fly.toml` for deploy skeletons.
