# Deferrals — Single Source of Truth

This is the **one canonical local home for every deferred item** in Loreforge: things
intentionally cut, postponed, stubbed-at-tracer-depth, or scoped out of the current
milestone. If something is "we'll do it later," it belongs here.

**Status (2026-06-21):** local-canonical. This file is the source of truth. GitHub
issues are mirrored **opportunistically** (not required) — see "Tracking" below. Issues
#1–#50 are closed (prior milestones). The **M5-frontier slices are mirrored to GitHub
issues #54–#68** (rows with a `#NN` in Tracking); the rest are `doc-only`. PRs #52
(docs), #53 (generator pipeline), and #69 (#54 rich Settlement schema) are merged.
**#54 done** (rich Settlement schema + tabbed detail); **#56 done** (character schema
extension — XP/equipment/spells/portrait/notes + character↔campaign membership);
**#64 done** (rich Tavern generator on the #54 pattern, cascade-enabled);
**#65 done** (rich Shop generator on the #54 pattern, cascade-enabled);
**#66 done** (rich Building generator on the #54 pattern, cascade-enabled);
**#67 done** (rich Faction generator on the #54 pattern, cascade-enabled);
**#68 done** (rich Dungeon generator on the #54 pattern, cascade-enabled) —
all 7 descriptive Realms types are now rich/sectioned except Region (GENR-6);
**#55 done** (campaign workspace shell + Overview tab at `/campaigns/[id]`);
**#61 done** (campaign workspace Party tab on the membership link);
**#60 done** (campaign workspace World tab + per-campaign discovery, migration `0009`);
**#59 done** (campaign workspace Plot Hook Kanban + accept-from-Realms lifecycle, migration `0010`);
**#62 done** (campaign creation flows — Quick Forge / Guided Setup / Empty World);
**#57 done** (Live Play narrative chat + player input modes, server-authoritative chat on the shared Yjs doc);
**#63 done** (Live Play compact exploration HUD + party-rail mini-HUD; full right-rail combat HUD removed in #214);
**#58 done** (Live Play full combat loop: map target picker + range rings, combat overlay, engine-resolved attack/cast, timed opportunity-attack reaction prompt);
**CHAR-7/CHAR-9 done** (tabbed character sheet with Equipment + Spells + Notes tabs; XP-gated level-up with engine XP-threshold helpers);
**#98 done** (Live Play ↔ character-sheet bridge: persisted campaign roster seeds the live encounter via `buildPartyBattleCommands`; combat **turn bar** + party mini-HUD driven by real weapons / spells / consumables from the sheet via `campaigns.partyLoadout` + `lib/sheet-loadout.ts`);
**#96 done** (Live Play chat: real AI-GM narration via `@app/llm` env-gated in the WS server with the stub as fallback; chat persisted to a dedicated `chat_messages` table and re-hydrated on room load; GM-referenced on-scene entities render as `@Entity` chips);
**#97 done** (Live Play input: AI "thinking" indicator broadcast around AI-GM turns; Check mode routed through the engine via a new deterministic `ability_check` command + `decideCheck` orchestrator; `LiveRoom.apply` surfaces command summaries);
**#99 done** (Live Play combat: AoE aim picker — area spells place an `origin` cell with a live caught-target preview via the engine's `withinBurst`/`withinCone`, resolved authoritatively; engine-event chat rows enriched with resolution detail (roll-vs-AC, damage, save failures/DC) from the command summary);
**#111 done** (Live Play combat: autonomous enemy/NPC turns — a `runEnemyTurns` WS-server driver runs every non-player turn through a deterministic `planMonsterTurn` planner (engine-validated move-to-reach + basic melee), with an optional #97-style LLM target pick (`decideMonsterTarget`) + per-turn GM narration (`narrateEnemyTurn`); combat is now two-sided even without an API key);
**ENG-15 done** (engine weapon attack range: `AttackCommand.rangeFt` → `OUT_OF_RANGE` in `handleAttack`; client/OA/readied/enemy AI thread weapon reach);
**#104 partial** (Live Play reactions — (1) AI-reactor opportunity attacks: a `runEnemyReactions` WS-server driver auto-takes the engine `opportunity_attack` for every AI-controlled reactor when a PC flees its reach; (2) Ready actions: a "Ready ▾" action-bar control arms a pick-any-foe target picker that holds a strike via the engine `ready_action` (trigger encodes the weapon range as `in_range:<ft>`), and a `runReadiedTriggers` WS-server driver fires it via `trigger_readied` the instant that foe advances into range on its turn (can interrupt the foe's own attack) — both with enriched resolution rows (`Readied — …`) + narration; pacing controls remain — issue stays open).

## How to use this file

- **Adding a deferral:** append a row to the relevant table with a stable `ID`. Don't
  delete rows when an item ships — change its **Status** to `Done` (so history is kept).
- **Tracking column:** `doc-only` = tracked here only. An issue number (e.g. `#57`)
  means it's also mirrored to GitHub. Use the `to-issues` skill to mirror a batch.
- **Phase tags** point at `docs/02-implementation-roadmap.md` §6 (P0–P7) and the
  milestones M1–M10. They are **best-effort** placement, not commitments.
- **Don't restate locked cuts** — v1.5+/v2 product cuts live in
  `docs/product-spec.md` §6 and `docs/00-consolidated-plan.md`; §8 below links to them
  rather than copying.
- This file supersedes scattered "What Is Open" / deferral prose. `AGENTS.md` and
  `CONTEXT.md` should point here for the live backlog.

**Legend — Status:** `Deferred` (planned, not started) · `Partial` (started, materially
incomplete) · `Stub` (placeholder/tracer-depth only) · `Done` (shipped; kept for history).

---

## 1. Generator pipeline (Realms AI) — plan `realms_generator_pipeline_50e6bfcd` (D1–D11)

The shared generator pipeline shipped at **tracer depth** (commits `90d7a9b`,
`230d97d` on `feat/realms-generator-pipeline`). `@app/llm` package, `generation_events`
audit table (migration `0007`), `realms.generate` / `expandStub` / `regenerate`
(field-subset capable) / `generateCascadeAsync` / `cascadeRun`, and the Advanced Form at
`/realms/generate/[type]` are all built. The items below are the parts deliberately
deferred from that design.

| ID | Item | Source | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|---|
| GEN-1 | Rich per-type schemas (replace thin `REALM_FIELDS` with rich per-tab schemas) | D1 | P4 / M5 | #54 | Done | **Done — all descriptive types now ship rich sectioned schemas** driven by the one descriptor model (`section` + `list`/`group` kinds; one source of truth → form / tabbed-detail / zod / generator tool-schema+prompt). Shipped per type: Settlement (#54), Tavern (#64), Shop (#65), Building (#66), Faction (#67), Dungeon (#68), Region (#116); NPC is mechanical (separate). Remaining work is per-type **depth & art/maps** tracked on the `GENR-*` rows (e.g. GENR-7 full ~19-tab Settlement + per-district map, faction crests, dungeon Dyson maps / rooms-as-entities, shop engine transactions) — not thin-schema work. |
| GEN-2 | Name-match dedup on cascade child-stub insertion | D6 | P4 / M5 | doc-only | Deferred | Cascade currently inserts every `children[]` entry as a new stub; no dedup against existing entities. |
| GEN-3 | OpenAI fallback provider (resilience) | D9 | post-M5 | doc-only | Deferred | Anthropic-only for v1; provider seam exists in `@app/llm` for later wiring. |
| GEN-4 | Full pgvector RAG grounding for generation | D11 | P5 | #141 | Done | **Shipped (#142) via MEM-6:** schema enums + curated SRD lists + parent context, now plus owner-scoped `retrieveSimilar` over existing `realm_entity` embeddings injected into the new/expand/regenerate prompts (`server/memory/related-lore.ts`). Offline-safe; env-gated on `OPENAI_API_KEY`. Cross-link inference shipped as a rerank source type (GEN-5, #167). See §6 MEM-6. |
| GEN-5 | Auto-link / conflict detection on generated entities | D6 / roadmap P4 | P4 / M5 | #167 | Partial | **Cross-link memory source shipped (#167):** relationship edges are now a retrievable `cross_link` source type in the live-turn rerank — each edge renders to a sentence (`composeCrossLinkText`, e.g. "Eldermoor (settlement) is located in The Mistlands (region)."), embedded owner-scoped, weighted just under direct lore (0.9, prefixed "Connection:") in `ws-server/world-knowledge`. Embedded on `realms.link` (best-effort, env-gated), deleted on `unlink`, and backfilled by `reembedCrossLinks` (catch-all for cascade/pre-existing edges) wired into the `backfill:embeddings` script + nightly `reembed-entities` Trigger task. So the GM can pull in connections, not just entity cards. **Still deferred:** a generator-time conflict/auto-link modal (surfacing/deduping inferred relationships at generation; see REALM-7). |

---

## 2. Unbuilt generators

Roadmap P4 prescribes 7 rich generators. NPC + Region + Settlement exist at **tracer
depth on thin schemas**. The generic Advanced Form can emit any type onto thin schemas,
but there are **no dedicated rich generators** for the five below. Build order per
roadmap §6 P4 (Tavern → Shop → Building → Faction → Settlement → Region → Dungeon).

| ID | Item | Source | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|---|
| GENR-1 | Tavern generator (menu + floor plan patterns) | roadmap P4 #2 | P4 / M5 | #64 | Done | **#64**: rich sectioned Tavern schema (Overview/Atmosphere/Menu group/Patrons & Amenities/Lore & Rumors) on the #54 pattern; tavern is now a cascade parent (emits tavernkeeper + patron NPC stubs + edges); generate/expand/regenerate + tabbed detail + per-section regenerate all work. Interactive floor-plan map deferred (REALM-5 / maps track). |
| GENR-2 | Shop generator (inventory ↔ engine transactions) | roadmap P4 #3 | P4 / M5 | #65 | Partial | **#65**: rich sectioned Shop schema (Overview/Inventory group/Quirks/Loot & Security/Lore & Rumors) on the #54 pattern; shop is now a cascade parent (emits shopkeeper + supplier/apprentice NPC stubs + edges); generate/expand/regenerate + tabbed detail + per-section regenerate all work. Inventory items model the buy/sell seam but **engine transactions (currency/inventory mutation), CUSTOM→Smithy promotion, and price auto-scaling are deferred** to play integration; interactive floor-plan map deferred (REALM-5 / maps track). |
| GENR-3 | Building generator (floor plans, custom sections) | roadmap P4 #4 | P4 / M5 | #66 | Partial | **#66**: rich sectioned Building schema (Overview/Architecture/History/Lore & Secrets) on the #54 pattern; building is now a cascade parent (emits owner + occupant NPC stubs + edges); generate/expand/regenerate + tabbed detail + per-section regenerate all work. Interactive floor-plan map deferred (REALM-5 / maps track). |
| GENR-4 | Faction generator (crest, relational graph) | roadmap P4 #5 | P4 / M5 | #67 | Partial | **#67**: rich sectioned Faction schema (Overview/Goals & Methods/Organization/Relationships/Lore & Secrets) on the #54 pattern; faction is now a cascade parent (emits leader + key-member NPC stubs via `member_of` edges); ally/rival lists complement the existing Realms relationship graph; generate/expand/regenerate + tabbed detail + per-section regenerate all work. Bespoke crest/emblem art deferred (art track). |
| GENR-5 | Dungeon generator (rooms as entities, Dyson map, encounter promotion) | roadmap P4 #8 | P4 / M5 | #68 | Partial | **#68**: rich sectioned Dungeon schema on the #54 pattern; cascade parent; generate/expand/regenerate + tabbed detail. **DUN-1–8 shipped** (engine spine + prep editor skeleton). **Active frontier:** §2.1 **DUN-9+** (SRD taxonomy, Codex pickers, map placement, multi-floor, Dyson). **Still deferred at GENR-5 umbrella:** rooms-as-entities IA (**DUN-19**), Dyson SVG (**DUN-15**), Combat-tab promote (**DUN-18**). |
| GENR-6 | Rich Region generator (deepest cascade, rich tabs) | roadmap P4 #7 | P4 / M5 | #116 | Done | **Spine slice A2 shipped:** rich sectioned Region descriptor (Overview / Geography & Climate / Settlements & Sites / Powers & Conflicts / Lore & Hooks) with settlement + site + faction groups; legacy terrain/climate/features keys preserved; region cascade child hint (settlements + dungeon + faction + NPCs). All 7 descriptive Realms types are now richly sectioned. |
| GENR-7 | Rich Settlement generator (richest tab set) | roadmap P4 #6 | P4 / M5 | doc-only | Partial | Settlement rich schema expanded (Jun 2026): Government, History, Law & Order, Culture, Economy detail, District Details group (~15 sections). Generate/expand/regenerate run against it. **Still deferred:** per-district map, full ~19-tab generator prompt depth, linked child entity stubs on detail. |

### 2.1 Dungeon authoring frontier (DUN-9+, post–DUN-8)

Ordered tracer slices for **GENR-5** depth + **PLAY-7** prep authoring. Ship in sequence; do not start the next until the current slice verifies (UI → prod; engine-only → CI).

| ID | Slice | Type | Blocked by | Target | Status | Notes |
|---|---|---|---|---|---|---|
| DUN-9 | SRD 5.2.1 dungeon tab taxonomy | AFK | — | Realms dungeon detail + form | **Done** | **#365:** Monsters / Gameplay Toolbox (5 topics) / Exploration Hazards tabs; legacy `hazards` + `wanderingMonsters` passthrough; room **Floor** field. |
| DUN-10 | Codex creature pickers (rooms + patrols) | AFK | DUN-9 | Realms edit form + engine foe resolution | **Done** | **#365:** Search Codex on room Encounter + Wandering Monsters; `encounter-ref.ts` slug resolution; patrol emit uses structured refs. |
| DUN-11 | Codex item + Gameplay Toolbox pickers | AFK | DUN-9 | Realms Rooms + Toolbox tabs | **Done** | **#366:** Search Codex on room **Treasure** (`treasureCodexSlug`) + all five Gameplay Toolbox groups (`{ label, codexSlug }`); legacy prose lists migrate on read; generator prompt nudges slug emission. Smithy homebrew pickers deferred. |
| DUN-12 | Map placement — traps, loot objects, NPC refs | AFK | DUN-11 | Realms `DungeonMapEditor` + `data.floors[]` | **Done** | **#367:** Loot / Trap / NPC map tools + Codex pickers; connection + zone-wide traps; `sceneTrapsFromFloor` bridges cell/connection traps into Live Play on `enter_dungeon`. Smithy refs deferred. Prod verify PASS (Greymark: Falling Net connection trap persisted). |
| DUN-13 | Zone rect drag-resize (non–4×4 rooms) | AFK | — | Realms map editor | **Done** | **#368:** Zones tool + 8 drag handles on selected zone rect; persists `zone.rect` (clears explicit `cells[]`). Prod verify PASS (Greymark: Hall of Names resized via sidebar + handles, saved, reloaded). |
| DUN-14 | Multi-floor + stair/transition authoring | AFK | DUN-13 partial | Realms map editor + `transitions[]` | **Done** | **#369:** + Add floor, floor picker, bidirectional `transitions[]`, Stairs tool + ↑/↓ markers. Prod verify PASS (Greymark: + Add floor, Stairs tool, floor picker live). |
| DUN-15 | Prep canvas zoom/pan + Dyson-style layout | AFK | DUN-13 | Realms map editor | **In progress** | **Branch `feat/dun-15-map-zoom`:** Wheel zoom + middle-click pan on prep map (shared `map-viewport-zoom` math). Dyson SVG import deferred (GENR-5). Prod verify pending. |
| DUN-16 | Prep fog paint | AFK | DUN-12 | Realms map editor | Missing | Per-PC fog preview deferred in DUN-8; paint starting fog / revealed cells in prep. Live fog shipped (DUN-5). |
| DUN-17 | In-play Edit Map | AFK | DUN-12, DUN-14 | Live Play (**PLAY-7**) | Missing | Owner prep edits during Live Session; tracked on PLAY-7 row. |
| DUN-18 | Combat-tab promote (Realms encounter → encounter library) | AFK | DUN-10 | Campaign Combat tab + Realms | Missing | One-click promote room encounter to saved campaign encounter (**CAMP-8**). |
| DUN-19 | Rooms as first-class sub-entities | HITL | DUN-12, DUN-13 | Realms IA + generator | Missing | Each room → linked Realms child entity / zone sync; generator spec §6. Requires IA grill for stub cascade vs inline. |

**Scheduling answers (Jul 2026):** Codex pickers (**DUN-10–11**) are **active frontier now** (P4 / GENR-5). Multi-floor/transitions (**DUN-14**), object/trap/NPC placement (**DUN-12**), object customization expand (**DUN-12**), and zoom/Dyson (**DUN-15**) were **implicit in GENR-5 / PLAY-7** prose but **not previously rowed** — this table is canonical. No committed calendar dates beyond **P4 / M5** phase; ship in slice order above.

---

## 3. UI-flow gaps (per-surface audit, 2026-06-21)

Each built surface was audited against its `docs/ui-flows/*.md`. Most surfaces are honest
**vertical slices** aligned with P1/P2/P3 scaffolding, not the production-complete flows
the docs describe. Gaps are grouped per surface; rows are deferral-worthy clusters, not
every micro-item (read the flow doc for full detail).

> **P4 backlog is now mirrored to GitHub issues #85–#105** (2026-06-21). The per-row
> `Tracking` cells below still say `doc-only`, but the live queue is on GitHub. Mapping:
> CHAR-3 → #85 · CHAR-7 (Combat/Features/Personality) → #86 · SMITH-5 → #87 ·
> REALM-2 → #88 · REALM-3 → #89 · REALM-5 → #90 · REALM-6 → #91 · REALM-7 → #92 ·
> CAMP-12 → #93 · CAMP-14 → #94 · CAMP-15 (Add-to-Campaign) → #95 ·
> PLAY-1 → #96 · PLAY-2 → #97 · PLAY-3/PLAY-6 sheet bridge → #98 ·
> PLAY-6 (AoE/resolution) → #99 · PLAY-4 → #100 · PLAY-5 → #101 · PLAY-7 → #102 ·
> PLAY-8 → #103 · PLAY-9 (Ready/AI reactions) → #104 · PLAY-13 → #105 ·
> PLAY-15 (enemy AI turns) → #111.
> (CHAR-9 is Done; not ticketed.)
>
> **M5-completion batch mirrored to GitHub (Jun 2026, see `docs/archive/03-m5-completion-plan.md`):**
> CAMP-8 → #115 · GENR-6 → #116 · CAMP-10 → #117 · CAMP-9 → #118 · CAMP-6 → #119 ·
> CAMP-7 → #120.

### 3.1 Home — `docs/ui-flows/home.md`

Built page is a P1 dev dashboard (surface cards + system status), not the cinematic
marketing/landing experience.

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| HOME-1 | Cinematic hero (imagery, AI-GM pitch, primary CTAs, scroll indicator) | P6 (pre-beta polish) | doc-only | Missing | Page is dev surface launcher. |
| HOME-2 | Marketing feature grid (Build Characters / Generate World / Run Campaign / Codex+Smithy) | P6 | doc-only | Partial | Current grid is dev links with "soon" badges. |
| HOME-3 | Logged-in personalization (Continue Your Adventure, recent character/campaign, Quick Level Up, Resume Session) | P4–P6 | doc-only | Missing | "Welcome back" is static. |
| HOME-4 | CTA banner, footer (app links + SRD attribution), Featured Builds carousel, SRD Fidelity badge | P6 | doc-only | Missing | — |
| HOME-5 | Global search, notifications bell, profile avatar dropdown, crest logo | P4–P6 | doc-only | Missing | App-shell nav chrome (shared across surfaces). |
| HOME-6 | Mobile hamburger nav fallback (nav links hidden < md with no fallback) | P6 (mobile pass) | doc-only | Missing | Shared app-shell gap. |

### 3.2 Codex — `docs/ui-flows/codex.md`

Spells-only MVP with search/filters/grid/slide-over detail. 9 of 10 categories stubbed;
no Smithy copy loop, deep links, right pane, or global search.

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| CODEX-1 | Non-spell category browsers (Rules, Species, Backgrounds, Classes, Animals, Monsters, Items, Feats, Advanced) | P3+ / ongoing | doc-only | Done | **All 10 categories live (#270)** — Advanced optional rules ingested from Open5e `/v2/rules/` (traps, poisons, diseases, madness, environment). Creation Wizard background selection remains out of scope (CHAR deferrals). |
| CODEX-2 | "Copy to The Smithy" from spell detail (+ bulk multi-select copy) | P2 (deepen) | doc-only | Partial | **Copy shipped (CODEX A4 #267):** all live Codex categories → Smithy via `smithy.copyFromCodex` (spells → grimoire; items → typed homebrew; others → editable text snapshots). **Still deferred:** bulk multi-select. |
| CODEX-3 | Deep links / bookmarkable detail (`/codex/spells/[slug]`, `?search=`, category routes) | P3+ | doc-only | Partial | **Path routes shipped (CODEX A1):** `/codex/{category}` list + `/codex/{category}/{slug}` detail; legacy `/codex?category=&slug=` redirects; `?search=` syncs on list views. **Still deferred:** global `/codex?search=` without category (redirects to spells). |
| CODEX-4 | Right pane (Recently Viewed, Quick Copy suggestions, Your Characters) + footer (SRD attribution, export) | P3+ | doc-only | Partial | **CODEX A3 (#266):** desktop right pane (Recently Viewed localStorage Codex+Smithy, Quick Copy popular spells → Smithy, Your Characters shortcuts); footer SRD OGL attribution + export bookmarks JSON. **Still deferred:** full section PDF/JSON export, account-synced recently viewed. |
| CODEX-5 | Richer spell filters (ritual, concentration, class-list) + List/Table view toggle + sorting | P3+ | doc-only | Partial | Level + school + **concentration/ritual** toggles; **class-list filter**, **Grid/List/Table** view toggle, and **column/sort dropdown** on Spells (**CODEX A2 #265**). **Still deferred:** same polish on non-spell categories. |
| CODEX-5b | Codex list filters persist when opening/closing detail (URL-synced filters + query preserved on detail nav) | P3+ | doc-only | Done | **`useCodexUrlParam` hooks** sync spell/item/monster/feat/advanced filters + pagination/sort/view to `?` params; **`codex-shell`** preserves full query string on detail open/close and cross-ref navigation. |
| CODEX-6 | Detail action buttons (Use in Character, Bookmark, Share, View SRD Source) | P3+ | doc-only | Done | **Shipped:** shared `CodexDetailActions` on all Codex detail modals — share link, local bookmark, Open5e SRD source, Copy to Smithy (**A4 #267**). **Use in Character (A5 #267):** Species/Classes/Backgrounds → Creation Wizard deep links; Feats → Characters dashboard. **Equip flows:** Prepare on character (spells) + Equip to character (items) from spell/item detail via character picker + `characters.update` merge helpers. **Still deferred:** account-synced bookmarks. |
| CODEX-7 | Global cross-app Codex+Smithy search bar | P4–P6 | doc-only | Missing | Shared app-shell gap (see HOME-5). |

### 3.3 Characters — dashboard / creation-wizard / inline-view / level-up

Functional P1/P2 vertical slices (create → view → edit basics → level up). Schema is
minimal (no XP, equipment, spells, portrait, notes, campaign links), which blocks several
features regardless of UI work. Router exposes only `list/get/create/update/levelUp`.

| ID | Item | Source | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|---|
| CHAR-1 | Character schema extension: XP, equipment/inventory, spells/slots, portrait, notes, campaign links | dashboard+view docs | P2 (deepen) / P4 | #56 | Partial | **Schema done (#56)**: migration `0008` adds xp/portrait/notes/equipment(rich)/spells(unified) + `campaign_characters` membership table; router handles new fields + add/remove/list membership; sheet round-trips XP + notes + portrait-URL stub. Full Equipment/Spells/Personality tab UIs are CHAR-7; portrait upload pipeline deferred. Unblocks CHAR-3/7/9, CAMP-3, SMITH-5. |
| CHAR-2 | Dashboard filter+sort bar, Grid/List toggle, card ⋯ menu (Duplicate/Export/Share/Archive/Delete), search | characters-dashboard.md | P2 (deepen) | doc-only | Partial | **Jun 2026:** search, name/level/**recently updated** sort, Grid/List, **card ⋯ menu** (Duplicate via `characters.duplicate`, Export JSON, Copy share link, Delete). **Still deferred:** Archive, bulk select. |
| CHAR-3 | Dashboard XP bars, Last Played, campaign grouping + Play Now, right sidebar, bulk select | characters-dashboard.md | P4 | doc-only | Partial | **`listDashboard`** joins campaign memberships; cards show **XP progress bars**, **Updated** date, **Play Now** chips, optional **group by campaign**, right **Quick tips** sidebar. **Still deferred:** true Last Played (session timestamp), bulk select. |
| CHAR-4 | Creation wizard steps 1,4,7,8,9 (Concept, Background, Equipment, Features/Spells, Flavor) — 6 of 10 built | character-creation-wizard.md | P2 (deepen) | doc-only | Partial | **10 steps + depth (#261):** full **Review** summary (level, advancement, equipment list, flavor), **multiclass level split**, **fighting style / subclass** picks, **ASI or feat** on Advancement, **milestone XP** on create. Background skills from Codex; origin feat recorded in sheet meta. |
| CHAR-5 | Wizard polish: full layout (right preview pane, footer), drafts/auto-save, undo stack, randomizers, 4d6 roll, success toast | character-creation-wizard.md | P2 (deepen) | doc-only | Partial | **Shipped:** right preview pane (real level/HP), footer nav, **localStorage draft** auto-save, **random name**, **Roll 4d6** ability method, **success toast** before redirect. **Still deferred:** undo stack. |
| CHAR-6 | Inline sheet: tabbed layout (8 tabs), right Live Stats HUD, left summary card, floating Save/Cancel toolbar, auto-save | character-view-inline-editing.md | P2 (deepen) | doc-only | Partial | **Roll20-style layout shipped (Jun 2026):** 3-column sheet (skills/tools left, tabbed center, defenses + Live Stats HUD right), header + HP panel, abilities bar, floating toolbar, notes-meta via `character-sheet-storage.ts`. Tabs: Combat, Spells, Inventory, Features & Traits, Notes, About. **Still deferred:** drag-and-drop, rich-text editors, full 8-tab doc labels (Core Stats / Skills & Saves split). |
| CHAR-7 | Inline sheet tabs requiring schema: Combat (attacks/death saves), Equipment, Features, Spells, Personality, Notes | character-view-inline-editing.md | P4 | doc-only | Partial | **Shipped:** Combat (Extra Attack, fighting styles, **feat toggles** Sharpshooter/GWM, **Open5e mastery lookup**), Features (**Use → engine** Second Wind/Action Surge, level history), **feat passives** (Alert/Tough/Mobile/Observant on HUD), Spells (Warlock pact + PHB pooled + **third-caster EK/AT slots**), milestone XP, engine cast. **Live Play:** `use_class_feature` (Rage/Frenzy, Monk Focus + Flurry loop, Sacred Weapon CD, Metamagic), attack flags (frenzy/flurry/Open Hand), `fast_hands`, **Cutting Words reaction prompt**, **`short_rest` + Natural Recovery sheet dialog**; **Still deferred:** full feat Codex ingest. |
| CHAR-8 | Level-up: 5-step wizard parity, Spells & Magic step, Review/confirm + celebration, feature/ASI/subclass application, version history | level-up-wizard.md | P2 (deepen) | doc-only | Partial | **5-step wizard + level history (#262).** **Added:** left sidebar summary + **localStorage draft** on level-up. **Still deferred:** full right live-preview pane parity with creation wizard. |
| CHAR-9 | XP-gated level-up (threshold check + post-level reset) | level-up-wizard.md | P4 | doc-only | Done | **Done**: engine `XP_THRESHOLDS` / `xpProgress`; sheet XP bar + gated Level Up. **Milestone mode (#261):** optional toggle bypasses XP gate and auto-syncs threshold on level-up. |
| CHAR-10 | Portrait upload/storage pipeline | rich-schema slice #56 (deferral-verify §21) | P4 / v1.x | doc-only | Done | **Done (#260)**: `characters.uploadPortrait` → Supabase Storage `character-portraits` bucket (`{userId}/{characterId}.ext`); sheet header click-to-upload (JPEG/PNG/WebP/GIF, 2 MB client cap) + URL fallback. Requires `SUPABASE_SERVICE_ROLE_KEY` + public bucket on prod. AI portrait gen still ART-1 / v1.5. |

### 3.4 Smithy — `docs/ui-flows/smithy.md`

P1/P2 slice: owner-scoped CRUD for homebrew **items** + declarative **spells** (create,
list, read, delete). Spells integrate with the engine (`validateSpellDefinition`); items
are metadata-only. Diverges sharply from the full "homebrew forge."

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| SMITH-1 | Edit existing homebrew (`update`/`updateSpell` mutations + edit UI/modal) | P2 (deepen) | doc-only | Done | **`smithy.updateSpell` + `smithy.update` + detail-page edit UI** (#269). Browse-card Edit via SMITH-3 `?edit=1` links. |
| SMITH-2 | Full Codex-mirror category sidebar (Species, Backgrounds, Classes, Animals, Monsters, Feats, Advanced) + unified "All My Homebrew" landing | P2 (deepen) | doc-only | Done | **`smithy.listLibrary` + sidebar (#270)** — All landing merges items + spells; category tabs filter Codex-mirror snapshots by `copiedFromSlug` prefix; Items/Spells retain forge/inscribe flows. |
| SMITH-3 | Browse-card actions (Edit, Duplicate, Delete-on-card, Use in Character) + timestamps + description snippets | P2 (deepen) | doc-only | Done | **`SmithyLibraryCard` + `duplicate`/`duplicateSpell` + browse Use in Character (#271).** Edit links to detail `?edit=1`; relative timestamps + description snippets on all browse cards. |
| SMITH-4 | Text search + filters (Last Edited, Source Copied/Original) + Grid/List/Table toggle | P2 (deepen) | doc-only | Done | **`SmithyBrowseToolbar` + `SmithyLibraryViews` (#272).** Search/source/sort on `listLibrary` + `listSpells`; grid/list/table across All, Items, and Spells tabs. |
| SMITH-5 | Character integration ("Use in Character" / Equip / Learn / Apply) | P4 | doc-only | Done | Sheet + detail + browse-card integration shipped (#198, SMITH-3). **Creation wizard Codex/Smithy gear + spell pickers (#273).** |
| SMITH-6 | Copy-from-Codex flow (real picker, `source`/`copiedFromSlug` populate, toast, Reset-to-SRD) | P2 (deepen) | doc-only | Done | **Spells + items copy picker, copy toast, Reset to SRD mutation (#273).** Category snapshots via existing `copyFromCodex`. |
| SMITH-7 | Item engine mechanics (`ItemDefinition`, weapon/armor stats, equipped effects; Codex→Smithy conversion; Live Play weapon resolution via `smithyItemId`) | P3/P6 | doc-only | Partial | **Tracer shipped:** engine `ItemDefinition` + `validateItemDefinition`; `homebrew_items.definition` jsonb; Smithy form/detail mechanics panel; `partyLoadout` fetches definitions; `deriveWeaponAttacks` prefers Smithy defs over name catalog. **ITEM-DEF-v2 (Jun 2026):** cost/weight, structured `propertyDetails`, range long, mastery, category on copy; Smithy detail property cards mirror Codex. **DATA-1a (Jun 2026):** `deriveEquippedArmorClass` + sheet `effectiveSheetVitals` reads equipped Smithy armor. **Deferred:** full `EffectTemplate` runtime (on-hit riders apply in combat), name-only armor without `smithyItemId`, QuickJS sandbox (ENG-4/P6). |
| SMITH-PROPS | Smithy forge: SRD weapon property + mastery checkbox grid → `propertyDetails[]` | smithy-item-form.tsx | P2 Smithy | doc-only | **Done** | **`SmithyPropertyPicker`** + `item-property-catalog.ts`; forge/edit writes structured properties; comma field retained for non-weapon types. *(Chat “SMITH-9” collided with export/import row — use **SMITH-PROPS**.)* |
| CODEX-SUB | Codex SRD subclass browse (curated seed, class detail links, subclass detail + Copy to Smithy) | P1 Codex | doc-only | Done | **`codex_subclasses` + migration 0038;** `seedSubclasses` via nightly character-options job; `listSubclasses` / `getSubclass` / `getSubclassByName`; subclass chips on class detail; typed `optionContent.subclass` on Smithy copy. Full Open5e subclass ingest deferred. |
| SMITH-TYPED | Typed Codex→Smithy snapshots beyond spells/items (subclass, background, feat `optionContent`) | P3 Smithy | doc-only | Partial | **Shipped:** `ItemDefinition.optionContent` discriminated union; backgrounds/feats/subclasses copy with structured defs. **Deferred:** species/class/monster typed defs; Smithy edit UI for option content. |
| SMITH-TOOLBOX | Smithy Gameplay Toolbox forge/copy/edit (structured field editing for all 5 topics) | P3 Smithy | doc-only | Done | **Removed from Smithy UI** (#306): no Toolbox nav, no Copy to Smithy, `/smithy/toolbox/*` → Codex. **Codex SRD 5.2 read-only** for traps/poisons/curses/env/fear. Live Play uses Codex slugs + engine seeds. **`SmithyToolboxForm` + `homebrew_toolbox_entries` retained** for future **GRILL-SMITHY-EDIT**. See `docs/data-model-hazards.md` §GRILL-SMITHY-EDIT. |
| CHAR-UX | Characters dashboard: card click navigates, inline delete, remove Open sheet | characters-dashboard.md | P2 | doc-only | Done | Whole card → `/characters/[id]`; trash confirm on card; ⋯ menu keeps Duplicate/Export/Copy link. |
| CHAR-UX-2 | Character sheet skills rail: widen left column, fix skill name/`?` tooltip clipping | character-view-inline-editing.md | P2 | doc-only | Done | Left rail **280px**; skills row uses flex + `break-words`; **`SrdHint iconOnly`** separates tooltip from truncated name cell. |
| CHAR-INV-1 | Inventory tab: remove/delete item rows | character-view-inline-editing.md | P2 | doc-only | Done | Per-row **✕** remove; **−** at qty 1 removes row from draft (save to persist). |
| CHAR-SPELL-GATE | Codex spell picker gated by class list + castable level | character-view-inline-editing.md | P2 | doc-only | Done | Engine **`spell-eligibility`** helpers; picker class dropdown + level chips + server search; blocks ineligible spells on add. |
| CHAR-FEATURES-v2 | Features tab: Codex subclasses, subclass feature rows, fighting-style tooltips, background benefits + origin feat, feats/ASI display | character-view-inline-editing.md | P2 | doc-only | Done | Codex-only subclass picker; **`listSubclasses.features`** expanded in Class Features; **`FIGHTING_STYLE_DESCRIPTIONS`** tooltips; background skill/tool/origin-feat cards; **`getFeatsByNames`** + ASI from **`levelHistory`**. |
| CHAR-ADV-1 | Creation/level-up advancement fixes: ASI feat modal, Codex subclasses only, spell picks, Finish button, ranger terrain/enemy | character-creation-wizard.md | P2 | doc-only | Done | PR #332 — **`ClassFeatureChoicePanel`** + engine choice registry for Divine Order, Deft Explorer, Land terrain, etc. **`CodexFeatAddPicker`** + `asiEligible` filter; **`SubclassPicker`** Codex-only; advancement **Finish** advances wizard; spell picker on caster advancement. **Deferred:** Metamagic, invocations, etc. |
| CHAR-ADV-2 | Creation/level-up dogfood fixes: Features step Next (level>1), feat prereq gating, subclass detail modal, level-up spell picker + ranger choices, multiclass validate/undo | character-creation-wizard.md / level-up-wizard.md | P2 | doc-only | Done | **`featuresStepChoicesComplete`** (subclass deferred to Advancement); **`featMeetsPrerequisite`** + **`multiclassEligible`** engine helpers; subclass **ⓘ modal**; level-up **CodexSpellAddPicker** + **`addedSpells`** on mutate; multiclass cancel panel + SRD ability gates. |
| CHAR-ADV-3 | SRD 5.2 subclass audit + creation parity: fighting style pick/persist, species traits, multiclass toggle, rich subclass features | character-creation-wizard.md / level-up-wizard.md | P2 | doc-only | Done | **One SRD subclass per class** + **`SRD_SUBCLASS_FEATURES`** seed; Ranger fighting style L2; advancement-step style pick + **`fightingStyles` meta persist**; **`SubclassCatalogPreview`**; species trait descriptions; **`listSubclasses` SRD-only**; multiclass click-to-deselect; legacy **`normalizeSubclassName`** aliases. **Post-merge:** run `npm run seed:character-options` in `packages/db` on prod. |
| SMITH-8 | Right pane (Recently Forged, Quick Copy, Your Characters), folders/tags, color-coded rarity | P3+ | doc-only | Partial | **Right pane + rarity badge colors (#273).** Folders/tags still deferred (needs persisted tag model). |
| SMITH-9 | Export/Import homebrew JSON, auto-save drafts, version history, undo | P3+ | doc-only | Missing | — |
| SMITH-10 | Forge theming/hero (imagery, animations, "Forged" badge, empty-state illustration) + monster/animal stat-block builders | P6 | doc-only | Missing | — |

**DATA-MODEL-AUDIT program** — canonical content-type gaps beyond spells/items (Jun 2026). Sub-rows track phased delivery; PDF-first for SRD mechanics.

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| DATA-1a | Armor AC from equipped `ItemDefinition` on character sheet | SMITH-7 | engine + sheet | Partial | **PR #285:** `deriveEquippedArmorClass` + Smithy/Codex `resolveItemDefinitions`; `codexSlug`; Live Stats HUD; inventory Inspect; Codex data-definition panel. **Prod verify pending** on Vercel prod after deploy. |
| DATA-1b | Gameplay Toolbox entries (traps → poisons → …) | P3 engine + Codex | engine + DB + UI | Partial | **All 5 toolbox topics seeded** in Codex (traps #286 … fear #290). **Live Play v1 ✅** (#292–#305). **Smithy toolbox removed** (#306). **GRILL-EXPLORATION ✅ Slices 1–3 shipped** (#307 Codex, #308 engine, #309 Live Play). **Deferred:** GM trap placement UI (prep-side `SceneState.traps` placement — Live Play detect/disable already shipped). |
| GRILL-EXPLORATION | Playing the Game → Exploration hazards + Rules Glossary (≠ Gameplay Toolbox) | P3 engine + Codex + Live Play | doc-only | **Done** | **Grill complete** (Q1–Q9). **Slice 1 Codex ✅** (#307). **Slice 2 Engine ✅** (#308). **Slice 3 Live Play ✅** (#309). Out of scope: poison contact/inhaled, curse contagion, env hourly/travel, dehydration/malnutrition/suffocation engine. See `docs/data-model-hazards.md` §GRILL-EXPLORATION. |
| DATA-1c | `MonsterDefinition` + Codex bestiary → engine bridge | P3/P6 | engine types | Partial | **`monster-definitions.ts`** validation stub only — not credible for encounter builder until Open5e monster → `MonsterDefinition` ingest + grill on action/legendary blocks. CAMP-8 uses curated `MonsterTemplate` only. |
| DATA-1d | Consumable / wondrous items without weapon shape (acid vial, etc.) | SMITH-7 | engine | Missing | `validateItemDefinition` weapon-centric; needs consumable/hazard item kind or relaxed validation for prose-only Codex copies. |

### 3.5 Realms — `docs/ui-flows/realms-library.md`

Audited in the previous chat. Grid/List/Graph toggle, type sidebar, stub badges, full-page
detail, Expand-with-Generator, generate→navigate, Advanced Form at the spec path +
"Surprise Me", per-section regenerate are **Followed**. Gaps below.

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| REALM-1 | Rich tabbed detail pages (currently flat field list / stat block) | P4 / M5 | #54 | Partial | Tabbed-detail shell built (#54): Settlement renders sectioned tabs with per-tab + per-field regenerate. Single-section types stay flat; remaining types get tabs as their rich schemas land. |
| REALM-2 | Hero stats strip on detail pages | P4 | doc-only | Missing | — |
| REALM-3 | Detail right pane (Recently Forged / Used in Campaigns / Quick Cascade / Style) | P4 | doc-only | Missing | — |
| REALM-4 | Per-tab Quick Generate bars with type-specific fields (today: one generic panel) | P4 / M5 | #54 | Partial | Advanced Form now skips list/group and seeds scalar preferred-values; per-tab Quick Generate bars still pending. |
| REALM-5 | Live Map Preview right rail | P4 | doc-only | Missing | — |
| REALM-6 | Cinematic multi-stage generation loader | P4 | doc-only | Missing | — |
| REALM-7 | Bulk ops; conflict/auto-link modal; auto-art; global search | P4 | doc-only | Missing | Auto-link = GEN-5; auto-art = ART-1 (v1.5 for AI gen). |
| REALM-8 | JSON import/export; World Anvil import | v1.5 | doc-only | Missing | World Anvil import is v1.5 per `realms-library.md`. |
| REALM-9 | List/graph scale (pagination + layout cost) | P3 Realms shell (deferral-verify §19) | post-shell scale slice | doc-only | Deferred | `realms.list` / `realms.graph` are **unpaginated** (load all owner entities) and `layoutGraph` is **O(n²)** — fine at shell scale, not for large worlds. Also: NPC detail edits via a full Edit *form* toggle (read-only stat block otherwise), not Character-View-parity inline click-to-edit. |

### 3.6 Campaigns workspace — `docs/ui-flows/campaigns-workspace.md`

**Canonical IA (Jun 2026):** [`docs/ui-flows/unified-campaign-ux.md`](ui-flows/unified-campaign-ux.md) — two shells (prep `/campaigns/[id]` + play `/campaigns/[id]/play`), seven-tab prep nav, center map-above-chat play layout, stub-scoped encounters, hybrid map hierarchy. Grilled and locked; supersedes conflicting sections in `campaigns-workspace.md` and `live-play-surface.md`.

**The `/campaigns/[id]` workspace shell exists** (#55, refactored to CAMP-UX): a
**seven-tab prep bar** (Overview / Map / Locations / Party / Quests / Notes / Settings)
with the Overview tab populated from real data; sessions & memories surface as Live
Play lightboxes. Opening a campaign (from the browser or after create) goes to the
workspace, not straight to `/play`. *(Historical #55 rows below describe the original
nine-tab shell — superseded by CAMP-UX UX-2.)*

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| CAMP-UX | **Unified Campaign UX** — prep/play shells, 7-tab prep, center `[Current\|World]` map, lightbox nav, collapsible character rail, territory painting, stub-scoped encounters, `startingSceneId`, Realms edit lightbox | M6.5+ | doc-only | Partial | **Spec locked** Jun 2026 in `docs/ui-flows/unified-campaign-ux.md`. **UX-1–7 shipped** (#234–#240). **Play polish shipped (#241):** unified Party right rail, always-on A1 grid coords, L0 + region/settlement map scale. **Next:** CAMP-14 player prep subset → … |
| CAMP-1 | Workspace shell at `/campaigns/[id]` (header, sidebar, tab bar, right pane, footer) | P4 / M5 | #55 | Partial | **#55** (since refactored to **CAMP-UX**): shell built — header (back link, Live indicator, title, Play Now/Continue) + **seven-tab prep bar** with `?tab=` deep links (legacy `world`/`hooks`/`combat`/`sessions` slugs redirect). Collapsible left sidebar, contextual right pane, and footer/toast layer not implemented (the prep shell is header + tab body). |
| CAMP-2 | Tab 1 Overview (continue/next session, party/world/hooks/NPC/session widgets, activity feed, pinned memory) | P4 / M5 | #55 | Partial | **#55**: Overview shows hero (name + pitch with inline edit via `campaigns.update`), Start Live Session + Continue entry points, a party summary + count from the membership link (`campaigns.party`), world/hooks/sessions stat cards (stubbed), and created/updated metadata. Activity feed and AI next-step hints deferred (CAMP-13); **pinned AI-memory highlights now shipped** as the Pinned Memory panel (MEM-8, #155). |
| CAMP-3 | Tab 2 Party (campaign-scoped character dashboard, companions, shared resources, bench) | P4 / M5 | #61 | Partial | **#61**: Party tab — add/remove roster via `characters.addToCampaign`; PCs / Companions / Bench cards + Live Play entry. **Mid-campaign roster sync shipped:** `sync_party` WS message + `add_combatant` / `create_entity` join commands when Live Play connects or roster grows (`buildPartyMemberJoinCommands`). Party-composition analysis, NPC-companion generator, shared currency/inventory pool, bench/deceased ⋯ menu deferred. |
| CAMP-4 | Tab 3 World (campaign-scoped Realms IA, discovery states, graph, add-from-Realms) | P4 / M5 | #60 | Partial | **#60**: World tab on a new `campaign_world_entities` table (migration `0009`) — add owned Realms entities into the campaign (`addWorldEntity`), per-campaign discovered/undiscovered toggle (`setWorldEntityDiscovered`, Q11), list + campaign-scoped relationship graph (`worldGraph`, edges filtered via shared `edgesWithin`), and a first-class `revealWorldEntity` auto-reveal seam for the AI narration pipeline. **Rung 3 (Jun 2026):** WS server auto-reveals undiscovered world entities when GM narration mentions them by name (explicit `mentions` + prose match via `world-reveal.ts` → `revealCampaignWorldEntities`). Flat-by-type sidebar IA, grid/list/graph filters, and live World-tab refresh deferred. |
| CAMP-5 | Tab 4 Quests (Plot Hook Kanban → unified Quest instances) | P4 / M5 | #59 | Partial | **#59**: Kanban + accept-from-Realms + detail panel. **Quest Phase A (#221, A.1 #222–#224):** tease runtime + cascade inheritance. **Quest Phase B/C (Jun 2026, prod verified):** tab renamed **Hooks → Quests**; Realms **Quests section** CRUD; accept snapshots template; Live Play **offer** + **briefing** + hot context. **Quest Phase D (Jun 2026):** hard prerequisite gates (`minLevel`, prerequisite quests) on accept/active/offer; step advance + branch UI; engine XP grant on Resolve; Realms editor tags/level/giver/prerequisites/rewards/branch steps; Campaign **List** view + tag/status filters; `quests` tRPC router (`hooks` alias). **#229 polish:** Accept `aria-label`, List shows pending Realms hooks, abandoned ≠ accepted for re-offer. **`on_combat_end` shipped (Jun 2026):** engine `resolveQuestAdvancesOnCombatEnd` + ws-server auto-advance on victorious combat end when step has `encounterRef`, `combat` tag, or `on_combat_end` trigger; GM briefing/complete line posted. **`on_step_complete` shipped (Jun 2026):** `resolveQuestAdvancesOnEvent` — auto-advance on enter location / talk to NPC when template trigger config matches; Quest tab `completeStep` queues GM briefing line for Live Play delivery. **Still deferred:** full branch graph editor, difficulty multiselect, `plot_hooks` table rename. |
| CAMP-6 | Tab 5 Sessions (log, recap cards, per-session Recap/Transcript/Combat/Events/Loot/Media) | P4 / M5 | #151 | Partial | **Shipped (#152, #214):** Sessions tab — ended-session recap cards + "End current session"; **deep view** with Recap / Transcript / Combat tabs + **Stats** summary (message/combat/roll counts). **Rung 3 (Jun 2026):** session **duration** on cards/detail; **editable recap** (`sessions.updateRecap` + re-embed); **Pin recap to memory**; Transcript **filters** (All/GM/Player/Dice/Events/OOC); **Events** audit tab; Stats **hooks touched** (approximate via session window); Start Live Session + Continue links. **Tracer:** Loot + Media tabs (placeholder copy until inventory/media pipeline). Full encounter replay, manual session entry, recap regenerate, and editable session titles deferred. |
| CAMP-7 | Tab 6 World Map (strategic pannable canvas, layers, edit mode, party token, discovery mirror) | P4 / M5 | #120 | Partial | **Tracer shipped:** `WorldMapTab` — pannable strategic canvas of campaign world entities with discovery fog (undiscovered → "Unknown site"). Wired into workspace `?tab=map`. **UX-3:** square overworld grid, territory painting (region/settlement), POI pins, prep toolbar, play World tab grid renderer; migration `0034_overworld_map`. **Still deferred:** cartographic art layer, layer toggles, party token, geo layout from Realms maps, create-on-map quick-generate. |
| CAMP-8 | Tab 7 Combat (encounter library + builder, difficulty, battle map, Run Now → Live) | P4 / M5 | #115 | Partial | **Spine slice A1 shipped at tracer depth:** `encounters` table + `campaigns.activeEncounterId` (migration 0012); `campaigns.encounters`/`createEncounter`/`deleteEncounter`/`runEncounter`; `MONSTER_TEMPLATES` catalog + `expandEncounterFoes`; the live room seeds the armed authored encounter (`getCampaignEncounter` → `CampaignRoom`); a Combat tab (catalog foe-roster builder + Run Now → Live). **Slice B shipped (#153):** deterministic **encounter difficulty (CR/XP) budgeting** — `cr`+`xp` added to the engine monster catalog; new pure `@app/engine` helper (`rateEncounter`/`encounterMultiplier`/`partyThresholds` + `ENCOUNTER_XP_THRESHOLDS`) implements the DMG procedure (foe XP × count-based multiplier, party-size nudge, vs summed party thresholds → Trivial/Easy/Medium/Hard/Deadly); Combat tab shows a live difficulty badge in the builder and per-saved-encounter (party levels from `campaigns.party` via `totalLevel`). Additive — no migration/WS change; golden unit-tested. **Rung 3 (Jun 2026):** battle-map **preset authoring** — `encounters.mapPreset` (migration 0031); three layouts (`ambush` / `arena` / `corridor`) via `ENCOUNTER_MAP_PRESETS`; Combat tab preview + Run Now seeds the chosen map. **Still deferred:** per-cell token placement, CR-budget suggestions, encounter-from-dungeon (GENR-5). **Rung-1 fix (Jun 2026):** Run Now passes `?arm=` to force a fresh Live Play WS connection; `CampaignRoom` re-seeds when the engine log is truncated externally while the room is cached; **Run Now clears persisted chat** (`clearCampaignChatLog`) so the narrative log matches the fresh fight. |
| CAMP-9 | Tab 8 Notes (editor, DM-only/shared, pin-to-memory, convert-to-hook, `@Entity` links) | P4 / M5 | #118 | Partial | **Spine slice A4 shipped:** `campaign_notes` table (title/body/`shared` flag, owner-scoped; migration 0014); `notes` tRPC router (`list`/`create`/`update`/`remove`); a Notes tab with a list + editor and a DM-only/shared toggle; `campaigns.delete` clears notes. **Pin-to-memory shipped (#161):** a "Pin to memory" button on the note editor calls `pins.create` with the note text (title+body, capped) → durable pinned memory (MEM-8). **Convert-to-hook + `@Entity` autolink shipped (#163):** "Convert to hook" calls `hooks.create` (title = note title/first line, summary = body); `@Name` mentions matching owned Realms entities render as links to the entity in a live "Linked entities" preview (`lib/notes-mentions.ts`, longest-match + word-boundary, unit-tested). **Still deferred:** rich-text rendering. |
| CAMP-10 | Tab 9 Settings (GM persona, art-style lock, play mode, members/invites, memory export, danger zone) | P4 / M5 | #117 | Partial | **Spine slice A3 shipped:** Settings tab with GM persona, default play mode (Q19c), art-style lock (Q16) on `campaigns` (migration 0013), + danger-zone delete (campaigns.delete clears campaign-scoped dependents). **Memory export now shipped (#157):** `memory.exportCampaign` tRPC (owner-scoped) assembles rolling summary + session recaps + pinned memories into a JSON document; a "Memory" section on Settings downloads it as `loreforge-<campaign>-memory-<date>.json`. **AI usage panel shipped (#212):** `llm_usage_events` table (migration 0030) logs ws-server orchestrator calls; `llm.usageSummary` tRPC + Settings "AI usage (30 days)" shows play + generation cost estimates; model routing via `ANTHROPIC_MODEL_ROUTING` / `ANTHROPIC_MODEL_NARRATION`. **Still deferred:** per-NPC TTS voices (PLAY-10); markdown/import formats for memory export. |
| CAMP-11 | Campaign creation flows (Quick Forge, Guided 6-step Setup, Empty World, template picker) | P4 / M5 | #62 | Partial | **#62**: three-path creation modal from the campaigns browser. **Empty World** creates a bare workspace; **Quick Forge** + **Guided Setup** (6-step stepper) create the campaign then run a synchronous Realms **region** (and optional faction) cascade via `campaigns.forge`, attaching the parent + cascade children to the World tab as undiscovered (reuses `generateNewEntity`/`persistChildren`). Forging is gated on `campaigns.forgeStatus` (ANTHROPIC_API_KEY) and degrades gracefully to an empty world when unconfigured; failures still land the user in the workspace. **#229:** post-create **party prompt** (create character / select existing / skip) + wizard `?campaignId=` auto-add. **Deferred:** durable Trigger.dev cascade with cinematic progress (needs `tr_prod_` key, INFRA-1) — currently synchronous; GM-preset/art-style fields; party-slot reservation for invitees; opening-scene narration generation; template picker. |
| CAMP-12 | Campaigns list: filter bar, rich cards (banner/roster/stats), card actions, templates section | P4 | doc-only | Partial | Today: name/description grid → play. |
| CAMP-13 | AI Memory panel (pins, recaps, entity awareness, drift detection) | P5 | doc-only | Partial | **Shipped:** Pinned Memory panel on Overview (MEM-8, #155) + in-play pin-to-memory; session **recaps** (CAMP-6 / MEM-4); **entity awareness** auto-reveal from GM narration (`world-reveal.ts`); a Live Play **Memories** panel (`memory.search`). **Still deferred:** drift detection; a dedicated unified memory-management surface; activity feed / AI next-step hints. |
| CAMP-14 | Multiplayer invite/seat flow (email/link invites, permissions, player-mode subset UI) | P4 | doc-only | Partial | **Tracer shipped:** `campaign_invites` + `campaign_characters.player_user_id` (migration 0029); tRPC `createInvite`/`listInvites`/`revokeInvite`/`getInvite`/`redeemInvite`; Settings → Players & invites; `/campaigns/join/[token]` redemption → Live Play; WS `canAccessCampaign` (owner OR seated player). **Slice 1 shipped (#242):** `campaigns.list` includes seated campaigns; `access.characterId` + seated PC resolution in Live Play; player cards → `/play`; reduced play-shell nav for players. **Still deferred:** email delivery, full player-mode workspace subset, per-connection entity command gate (GM→NPCs), permissions matrix. |
| CAMP-15 | Cross-surface entry ("Add to Campaign" from Characters/Realms; "Back to Workspace" from play); deep links `?tab=` | P4 | #55 | Partial | **#55**: "Back to Workspace" link added to Live Play; workspace tabs deep-linkable via `?tab=<slug>`. "Add to Campaign" from Characters/Realms still pending (Party tab #61 covers the in-workspace add). |
| RUNG-4 | Campaign bootstrap → first hour of play (**Realms ↔ Live Play** integration) | P4 / M5 | doc-only | Partial | **Priority over Ladder 3 tab polish** — see `docs/00-consolidated-plan.md` § Realms ↔ Live Play. **Slice 1 shipped (#215):** exploration bootstrap + GM opening line. **Slice 2 shipped (#216):** enter World-tab locations via chat travel (`matchTravelDestination`) or World tab **Enter in Live Play** (`?enter=` / `enter_location` WS message). **Slice 3 shipped (#217):** opening hook tease from Realms `data.hooks`, World-tab NPC tokens on exploration maps, dungeon combat-on-enter (`buildDungeonEntryCommands`). **Slice 4 shipped (#218):** GENR-5 room promotion — first dungeon room `encounter` → live foes + entry-room arrival line. **Quest Phase A (Jun 2026, #221):** tease path now uses structured quest templates + trigger evaluator (see `docs/quests.md` §10); legacy string hooks auto-migrate on read. **Quest Phase A.1 Done (#222, #223, #224):** cascade location stubs inherit parent region quests on forge + ws-server multi-hop `located_in` parent fallback; rebind inherited quest location ids so session-start tease fires at default tavern (prod verified). **Quest combat-end advance (Jun 2026):** victorious encounter end auto-advances combat-tagged quest steps + GM line (pairs with CAMP-5 `on_combat_end`). **PR #356 (superseded):** per-room scenes + `advance_dungeon_room` — **do not extend**. **DUN-1 Done (Jun 2026):** `enter_dungeon`, `mark_zone_cleared`, zone `dungeonProgress`, floor scenes, threshold spawn once; spec **`docs/engine/dungeon-exploration.md`**. **DUN-2 Done (Jul 2026):** zones layout loader (`rooms[]` synthesis + `floors[]`), `use_connection`, `use_floor_transition`, `DungeonLayoutSet`, position-derived `ZoneVisited`, connection gating — grill locks Q35–60 in spec. **DUN-3 Done (Jul 2026):** passive detection (`CreatureDetected` / `ZoneAlerted`), `start_zone_encounter` roster rules, invisible skip, zone-entry checks on move + floor transition, dungeon enter uses zone spawn — #359. **DUN-4 Done (Jul 2026):** `interact_object`, `ObjectTaken` projection, zone `objects[]` in layout loader, quest step `completionKind` (`enter_zone`, `interact`, `discover_zone`, `detected_in_zone`, `defeat_encounter`, `talk_npc`) — #360. **DUN-5 Done (Jul 2026):** per-PC cell fog (`dungeonFog`, `FogRevealed`, `ZoneDiscovered`), `share_scout_reveal`, `reveal_area`, Live Play fog overlay — #361. **DUN-6 Done (Jul 2026):** authored `patrolRoutes[]`, `tick_patrols`/`reset_patrols`, ws-server patrol timer + session reset, fog-gated patrol tokens — #362. **DUN-7 Done (Jul 2026):** generator emits authored `data.floors[]` on dungeon save (`enrichDungeonEntityData`, slug zones, tree layout) — #363. **DUN-8 Done (Jul 2026):** Realms prep `DungeonMapEditor` — wall paint, entrance, objects, connection locks; zone rect edit + fog paint deferred. |

### 3.7 Live Play — `docs/ui-flows/live-play-surface.md`

Early tactical-map slice (~15–20% of doc): PixiJS grid, Yjs sync, move + end-turn,
server-authoritative engine. Narrative chat, HUD, and most of the 5-zone shell absent
(explicit code stub: "...arrive with the Live Play surface (P4)").

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| PLAY-1 | Chat / narrative zone (GM/player entries, dice widgets, engine-event rows, entity chips) | P4 | #57 / #96 | Partial | **#57**: server-authoritative narrative chat on a top-level `chat` Y.Array in the shared Hocuspocus doc (clients are observers; `useLiveSession` exposes `chat` + `sendChat`). Renders GM / player / engine-event / `roll` / OOC rows; accepted battle commands emit `event` rows; `/roll NdM±K` renders a structured dice widget. **#96**: the stubbed GM echo is replaced by **real AI-GM narration** — the WS server calls `@app/llm` (env-gated on `ANTHROPIC_API_KEY`; falls back to the echo when unconfigured or on failure) with the live scene + recent chat, and the GM is a storyteller only (the deterministic engine still owns all mechanics, Q12). **Chat is now persisted** to a dedicated `chat_messages` table (per-campaign `seq` ordering) and re-hydrated on room load, so the conversation survives a cold reload; sandbox rooms stay ephemeral. The GM's referenced on-scene entities render as **`@Entity` chips**. **Deviation:** chat is persisted to its own table, *not* the deterministic `engine_events` log, because AI narration is non-deterministic and would break replay. **Deferred:** narration is synchronous (no "GM is thinking…" indicator — #97), mentions are limited to live on-scene entity names (no Realms/campaign-world-entity linking or auto-reveal — that rides the CAMP-4 reveal seam), and the engine isn't yet given narration context back as memory (rolling summary / RAG is P5). |
| PLAY-2 | Player input modes (Speak/Action/Check/Cast/Attack/Use Item, slash commands, OOC) + AI "thinking" state | P4 | #57 / #97 | Partial | **#57**: composer with the six mode buttons, slash commands (`/roll`, `/help`), and OOC `((…))`. **#97**: the **AI "thinking" indicator** is live — the WS server broadcasts a transient `{t:"thinking"}` stateless signal around every real AI-GM turn and the chat zone shows an animated "GM is weaving the tale…" row. The **Check mode now routes through the engine via the orchestrator**: a new deterministic `ability_check` engine command (d20 + ability mod + optional proficiency vs DC, emitting `CheckRolled`); the server asks the LLM (`decideCheck`) which ability/skill + DC a free-text attempt needs, the engine rolls it (the model never sees/invents the result), the outcome is shown as an engine-event row, and the GM narrates honouring the dice. `LiveRoom.apply` now returns the engine command summary so the server can read mechanical results. **Deferred:** Action/Cast/Attack/Use-Item composer modes still narrate (mode-aware) rather than emitting structured commands — Attack/Cast already have the mechanical path via the combat action bar (#58/#98); free-text→combat-command orchestration (LLM proposing attack/cast/use-item tool calls) is the remaining piece. The check is resolved against the active PC (or first PC in scene); per-skill proficiency is the orchestrator's call (the live entity doesn't carry skill lists). Check routing requires `ANTHROPIC_API_KEY`; unconfigured, Check narrates via the stub. |
| PLAY-3 | Character HUD (exploration compact + party mini-HUD; full right rail) | P4 | #63 / #98 / #214 | Partial | **#63 / exploration:** compact PC panel in the right sidebar when no encounter (name, HP/AC/speed, Sheet link). **#98:** combat **turn bar** lists real weapons/spells/consumables for the controllable party combatant. **#214 / Jun 2026:** during combat the right rail shows a **PC-focused Live Stats HUD** (`CharacterHud` with `entityId`, **`statsOnly`** — vitals/abilities/conditions only; Attacks, Action Economy, End Turn deduped to turn bar). Portrait, ability quick-roll (chat), HP-delta toasts; party chips click → sheet peek. **Deferred:** compact HUD mode polish, class-list spell filters on HUD, HP-change sound, full 8-tab sheet inline. |
| PLAY-4 | Party rail (collapsed chips, hover mini-HUD, assist pulses) | P4 | #100 | Partial | **Shipped (#125, #214):** party rail chips + hover mini-HUD + active-turn pulse; **click-to-open read-only sheet peek** (PLAY-4). **Still deferred:** cross-character **assist pulses** (Help/Bardic Inspiration — needs engine signal), multiplayer presence dots. |
| PLAY-5 | Full top bar (scene breadcrumb, dual clocks, Pause, tools row: pacing/TTS/memory/inventory) | P4 | #101 / #214 | Partial | **Spine slice B2 shipped (#126):** structured `live-top-bar` — breadcrumb back, campaign+scene label, Live/Async presence chip (peer count), scene name, live real-time session clock, client-side **Pause** (freezes local turn UI + map interactions + clock), **Reset**, optional **End session**, tools row with live **🎚 Pacing**. **#214:** during combat, **End turn** and round/turn/movement breadcrumbs moved out of the top bar (→ turn bar + initiative strip). **Still deferred:** in-game clock (no engine time field), server-side pause *freeze* of engine+AI, named connection roster (presence), and the tool panels — TTS (PLAY-10), Memory (P5), Inventory drawer (placeholders disabled). |
| PLAY-6 | Combat overlay (round banner, horizontal initiative) + full combat loop (attacks/spells/reactions/targeting/resolution) | P4 | #58 / #98 / #99 / #214 | Partial | **#58 / #214 layout:** initiative strip **full width above map + party row**; horizontal **turn bar above chat** (economy, Attack/Ready/Cast, quick-use, End turn, reaction prompts) — no combat right sidebar. Map target picker + movement radius on active token. **#58 mechanics:** attack + single-target spell cast resolved by the **engine** (`attackAction`/`castAction` added to the live `BattleAction` set, validated server-side). **#98:** turn bar lists the controllable PC's **real equipped weapons** (multi-weapon dropdown; SRD weapon catalog × live ability mods/prof, with ranged weapons setting the picker range) and **real castable spells** from the sheet (sheet spells ∩ registry subset, slot-gated, ENG-12 prepared gate); opportunity-attack reactions use the reactor's sheet weapon. **#99:** **AoE aim picker** — area spells place an `origin` cell with a live caught-target preview via the engine's `withinBurst`/`withinCone`, then **Confirm** sends a `cast_spell` with an `origin` cell the engine resolves authoritatively. **Resolution detail**: engine-event chat rows enriched from the command summary. **ENG-15:** weapon `rangeFt` enforced in `handleAttack` (`OUT_OF_RANGE`). **Deferred:** weapon proficiency assumed for equipped weapons; limited cast subset + area spells (Fireball / Burning Hands tracer); AoE preview ignores LOS; Ready shipped (#104); HP-change toasts; L4 auto-zoom; foes still fixture goblin ambush for sandbox. **Action economy enforced:** Attack/Cast/Ready disable once action spent; attacks budget via `attacksPerAction`; End turn in turn bar. |
| PLAY-7 | Map zoom levels L0–L4 + layer toggles + Edit Map + fog of war + token interaction menus + text-driven movement | P4 | #102 | Partial | **Spine slice B4 shipped (#128):** `MapViewport`: CSS-scale zoom (in/out/reset) + layers panel (grid + movement). **UX-7 shipped (#240):** shared zoom helpers; L0 wheel zoom on overworld grid; L3/L4 badges + Return-to-scene pill on Current map. **DUN-5 shipped (#361):** per-PC dungeon cell fog overlay in Live Play exploration (entry zone reveal on threshold; expands on move). **DUN-6 shipped (#362):** patrol tokens hidden outside revealed fog until detection. **DUN-8 shipped:** prep-only dungeon floor editor in Realms (walls, entrance, objects, connection locks). **Still deferred:** hierarchical **L1–L2** parent-map scroll-out on Current tab, full LoS fog model, **in-play Edit Map** authoring, token context menus, text-driven movement, prep fog paint. |
| PLAY-8 | Scene transitions (cross-fade, location banner, auto-forge stubs on travel) | P4 | #103 | Partial | **Spine slice B3 shipped (#127):** watch the synced `currentSceneId` → cross-fade the map + drop a location banner pill on scene change (initial load not animated); pure `isSceneChange` + `useSceneTransition` hook with unit coverage. **Design pass shipped:** chat scene-divider rows on location/combat transitions, scene-description subtitle on banner, combat start/end dividers. **Still deferred:** AI **auto-forge** of destination stubs on travel (generation pipeline), token move/spawn animations, dedicated in-game time/weather subtitle fields. |
| PLAY-9 | Tier 4 reaction windows (timed prompts, auto-pass), pacing controls | P4 | #58 / #104 | Partial | **#58**: timed opportunity-attack prompt — when the engine opens a reaction window (`encounter.reactionWindow`) for a party-controlled reactor, a 12s countdown prompt offers Opportunity Attack (engine `opportunity_attack`) or Pass; auto-passes on timeout; dismissed windows stay dismissed. **#104 (AI-reactor reactions)**: when a PC flees an **AI-controlled** reactor's reach, the orchestrator now takes the opportunity attack automatically — a `runEnemyReactions` driver in the WS server resolves every AI-eligible reactor in the open window via the engine's `opportunity_attack` (attack/damage from the reactor's `monsterAttackProfile`), emits the enriched resolution row, and narrates it under the "GM is thinking" signal; runs after any accepted player command, bounded by a guard. Pairs with enemy AI movement (PLAY-15/#111), so fleeing now has a cost both ways. **#104 (Ready actions)**: the combat action bar gains a "Ready ▾" control that arms a target picker over **every** hostile in the scene (not just those in range — a readied strike is for a foe that hasn't closed yet); picking one holds the strike via the engine `ready_action`, encoding the weapon's range in the trigger (`in_range:<ft>`). A `runReadiedTriggers` WS-server driver (mirroring `runEnemyReactions`) evaluates held actions after every enemy move and fires the one whose target has entered range via `trigger_readied` — *before* the foe's own attack resolves, so a readied blow can drop the attacker first. Resolution rows render `Readied — …`; a held action shows a confirmation banner on the owner's turn (engine clears it at their next turn). **#104 (pacing controls, B6 / #130):** the top bar's 🎚 Pacing button opens a real panel — persisted per-campaign **style** (Cinematic/Balanced/Reactive) + **round-timer limit** (Off/30/60/90s) and **Continue/Hold/Skip** quick controls (Continue/Skip nudge the AI via chat; Hold is a local indicator). A soft combat **round timer** shows per-turn elapsed in the top bar (ok→warn→over), resetting each turn and pausing with Pause/Hold. Pure `live-pacing` helpers + `usePacingPrefs`/`useTurnTimer`. With all three workstreams shipped, **#104 is closed**. **Deferred follow-ups:** initiative **delay** (engine re-slotting; *hold = Ready*, shipped), server-side **Hold** enforcement, pacing-style → AI narration density, round-timer auto-advance, and **multi-client reaction-window sync** (simultaneous reactor prompts broadcast over Yjs with a synced per-client countdown — today windows resolve per-controllable-reactor, not fanned out to multiple human players). Non-attack reactions (Shield, Counterspell, Absorb Elements) ride the ENG-13 effect/handler track. |
| PLAY-10 | TTS (toggle, per-NPC voices, Listen, queue) | P4 (TTS) | doc-only | Missing | TTS in v1; STT is v1.5 (V15-*). |
| PLAY-11 | Inline memory & retcon (panel, per-entry "Retcon from here", ghost-timeline confirm) | P5 | doc-only | Missing | Retcon UI is P6/E5. |
| PLAY-12 | End-session flow (stats summary, auto-recap, memory pin, redirect to workspace) | P4/P5 | #151 | Partial | **Shipped (#152):** "End session" button in the live play top bar (`onEndSession`/`endingSession`) wired via `sessions.end`; on settle redirects to the workspace Sessions tab where the recap appears. Auto-recap + memory embed happen server-side (MEM-4). **Memory-pin step shipped (#165):** post-session panel with recap + inline pin form on Sessions tab. **Live play pin modal shipped (Jun 2026):** `PostSessionPins` overlay on end-session from Live Play → Done redirects to Sessions tab. **Rung-1 polish (Jun 2026):** end-session errors surface inline on Live Play; recap transcript includes engine event rows (combat-only sessions); **async recap polling** on end-session modal + Sessions tab while Trigger.dev job runs. **Still deferred:** stats summary. |
| PLAY-13 | Async-play affordances (async badge, "X joined — Switch to Live?" prompt, resume banner) | P4 | #105 | Partial | **Spine slice B5 shipped (#129):** async/Live presence **badge** (in the top bar, B2), a dismissible **Switch-to-Live prompt** on the play surface when the peer count rises (≥2), and a **resume banner** on Overview derived from the persisted projection (`engine.state` → `resumeSummary`) that deep-links into `/play`. Pure `joinedSincePrompt`/`resumeSummary` with unit coverage. **Still deferred:** push notifications + a workspace-header "Live: Active — Join" chip, and per-player async turn-nudge notifications. |
| PLAY-14 | Live-play resilience/polish (reconnect banner, lag indicator, AI retry, cost meter, idle timeout, crash recovery, mobile play tabs) | P6 | doc-only | Missing | — |
| PLAY-15 | Combat AI Orchestrator — autonomous enemy/NPC turns | P4 | #111 | Partial | **#111 + Multiattack shipped:** `planMonsterTurn` spends the full `actionEconomy.attacks` budget after move-to-reach; ogre template `attacksPerAction: 2`; unit tests in `enemy-ai.test.ts`. **Ranged loadout (PR #231):** bandit light-crossbow profile on `MonsterTemplate`; `planMonsterTurn` fires at range without closing when foe is outside melee; seeded via `expandEncounterFoes`. **Spell turns (Jun 2026):** `cultist` template + `planMonsterSpell` — casters prefer highest-level prepared spell in range (Burning Hands / Fire Bolt) before melee; `MonsterTemplate.spellcasting` seeds `preparedSpellIds`. **Still deferred:** legendary/lair actions, encounter tactics hint, reaction spells. |

### 3.8 Onboarding tutorial — `docs/onboarding/tutorial-adventure.md`

The 30-min "Lantern's Last Flicker" tutorial micro-campaign is a **locked v1 deliverable**
(AGENTS.md, M6 gate). Not built yet (no `/tutorial` route), but the **implementation
architecture is locked** (Jun 2026) and sliced into 10 tracer-bullet issues (#169–#178);
see the `TUT-1` row for the D1–D8 decisions.

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| TUT-1 | "Lantern's Last Flicker" tutorial E2E (scripted scene state machine, first-time tooltips, graduation modal, replay) | P5 / **M6** | #169–#178 | Done | The capstone onboarding green-path; gates strict open-beta/GA. **Design locked (Jun 2026)** — see grilled decisions D1–D8 below; broken into 10 tracer-bullet slices (#169–#178). **D1** full reuse: real engine + per-user seeded **real** campaign (`is_tutorial`) + **extended** Live Play surface; thin scripted driver on top. **D2** new **exploration render mode** on the play surface (map+chat+HUD, no combat chrome) for a scene with a map but no encounter (today's surface is combat-gated). **D3** server-side **`TutorialRoom`** (ws-server) running a shared data-driven script; deterministic triggers (UI action + engine event) as the backbone, LLM only as a chat-intent classifier with keyword→hint→auto-progress fallback (air-gapped-safe). **D4** fixture-seeded, per-user, world+campaign are **real owned DB rows**; item-uses scripted. **D5** reusable anchored **coachmark primitive** (shared infra) + `tutorial_seen_features` fire-once table. **D6** scene-granularity resume via `tutorial_progress` singleton; engine state from event log; replay truncates engine + resets progress but keeps seen-tooltips. **D7** graduation always; `TUTORIAL_GATE` flag (`off`\|`first_run`, default off), `first_run` = forced splash with frictionless skip→starter (matches `product-spec.md` §5.2). **D8** defer to v1.5: tutorial TTS/voiceover (§11.1), multiplayer onboarding (§9.3), and the Adventure Recap **social-share generator** (keep graduation modal + static recap + the two achievements); funnel telemetry emits to the env-gated stub now. Slices: #169 tracer (route+seed+Scene 1, migration `0019`), #170 coachmark primitive, #171 Scene 2 (chips/drawer/hook/companion), #172 Scene 3 (shop/inventory), #173 Scene 4 (dungeon/advantage/loot→combat), #174 Scene 5 (Hungering Shade combat), #175 Scene 6 (item-use outcomes/level-up/pin), #176 Scene 7 (graduation/achievements/funnel), #177 launch gate, #178 resume/replay/hints/fail-forward/telemetry. **Shipped (Jun 2026):** #169–#175 merged to `main` — M6 Scenes 1–6 + the coachmark primitive. #175 (Scene 6 finale) lands the four-path lantern relight (Oil best-outcome consumes the item, flint standard, prayer = real engine Religion check w/ fallback, improv → standard), hook→Resolved, XP award clamping Mira up to her Level-4 threshold (level-up notice + tooltip, **no wizard** — see D-note below), narration-only reputation (real system deferred → REP-1), and inline pin-to-memory on GM chat messages. **Post-#175 playtest bugfixes (Jun 2026):** (1) **action economy** — combat let the player attack unlimited times per turn (engine never spent the action on attacks/casts), so the Shade died turn 1 and the fight wedged on concurrent drivers; fixed engine-wide (see PLAY-6). (2) **fire-once controls** — tutorial chips/buttons (Talk to Barnaby, Continue) re-posted GM beats on every click; now guarded both client-side (disable-after-use + an "advancing" latch) and server-side (`TutorialRoom` per-room action lock + `markOnce` dialogue dedupe). (3) **full reset** — Reset only truncated the engine log; now it also clears the chat (Yjs doc + `chat_messages` + rolling summary + pins), restores the seeded DB state (hook→suggested, companion→reserve, scene→1, hero XP + granted items de-progressed), re-posts the Scene 1 opening, and broadcasts a `reset` signal so connected tabs refetch + clear local guards. **Shipped (Jun 2026):** #178 resume/replay/hints/fail-forward/telemetry — Continue/Replay on splash, `tutorial.replay` + ws `reset` replay-from-start (coachmarks preserved), idle hint chip (>45s, 3 dismissals → auto-progress), fail-forward rails (friendly-fire deflect, leave-village soft rail, LLM-offline fallback), funnel telemetry stub. **Remaining:** none — all #169–#178 slices landed. **Shipped (Jun 2026):** #177 launch gate — `TUTORIAL_GATE` env (`off` default, `first_run` redirects brand-new users to `/tutorial` via app layout), frictionless skip seeds standalone Mira + `tutorial_progress.status = skipped` (nullable `campaign_id`, migration `0022`), gate decision tests in `tutorial-gate.test.ts`. **Shipped (Jun 2026):** #176 Scene 7 wrap & graduation — a `tutorial_achievements` table (migration `0021`) + the two tutorial-only achievements (**First Steps** unlocked by the web `tutorial.acceptHook` mutation in Scene 2; **First Light** by the new `tutorial.complete` mutation on finish), a full-screen **graduation modal** (`graduation-modal.tsx`) rendering the completion stamp, both achievement badges, the **static** recap (D8 — no social-share generator), and the "what's next" funnel routing to Create character / Quick Forge / Codex / Realms / Home. The ws-server posts a fire-once Scene 7 closing GM beat (`wrap` tutorial action + `TUTORIAL_WRAP` engine fixture) and broadcasts a `graduated` signal; finishing sets `tutorial_progress.status = completed`. *Minor follow-ups (not blocking):* the modal re-opens on every load of a completed run (D7 graduation-always — closeable); the "Replay the tutorial" button triggers a full replay-from-start; achievements are tutorial-only (the general achievement system stays out of v1, D8) and there is no Profile → Achievements surface yet; the action-bar Cast/Ready disable keys off the single action, so a bonus-action spell is also disabled once the action is spent (UX imprecision, not a rules break); the reset XP baselines are duplicated constants in `ws-server/db.ts` (kept in sync with the tutorial router's `TUTORIAL_MIRA`/`TUTORIAL_BRENNAR`). |
| REP-1 | Real reputation system (per-faction/settlement standing with mechanical reach; `ReputationChangedEvent`) | P5+ / post-M6 | #175 | Partial | **Tracer shipped:** `campaign_reputation` table (migration 0029); tutorial finale upserts Last Light Hollow → Honored (`awardTutorialReputation`); Settings reputation panel + `campaigns.reputation` tRPC. **Still deferred:** `ReputationChangedEvent` in engine, mechanical reach (prices/access), faction graph seeding from Realms, player-facing discovery UI beyond Settings. |

---

## 4. Engine / spells

| ID | Item | Source | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|---|
| ENG-1 | Postgres-backed per-campaign event persistence behind engine tRPC router | roadmap P1→P2 (old #2) | P2 | #2 (closed) | Done | Shipped: `engine_events` table + `PgEventStore`; `engine.submit`/`state` append + rebuild scoped to owned campaign. Kept for history. |
| ENG-2 | Top-120 spell authoring + golden tests (full curation) | roadmap E3–E5 | P3–P5 | doc-only | Partial | **124 spells** in registry (Jun 2026): top-120 batches 3–11 + **depth batch:** Misty Step, Dispel Magic, Counterspell, Polymorph. **Multi-component damage** (Ice Storm 2d8 bludgeoning + 4d6 cold). **Line/cube AoE** geometry (`withinLine`, `withinCube`; Lightning Bolt line, Sunbeam line). Golden snapshots updated. **Still deferred:** Polymorph beast form, Counterspell interrupt resolution, Dispel Magic CON save per effect, class-list curation beyond registry. |
| ENG-3 | Remaining ~240 SRD spells (toward full ~360) | roadmap §11 | post-GA / v1.x | doc-only | Deferred | Registry expansion, no schema break. |
| ENG-4 | QuickJS Smithy sandbox (imperative escape hatch execution) | roadmap E5 | P6 | doc-only | Deferred | "Sandbox deferred" in P2 Smithy MVP note. |
| ENG-5 | Retcon UI (ghost-timeline surfaced as undo/audit) | roadmap E5 | P6 | doc-only | Deferred | Event store is retcon-ready; UI not built. |
| ENG-6 | LLM tool-adherence harness (>98% on fixtures) | roadmap §7 alpha gate | P6 | doc-only | Partial | **C2 skeleton shipped** (`services/ws-server/src/adherence/`): fixture battery + graders for check/target/narrate orchestrators; vitest gate ≥98% on fake clients. **#212 expansion:** battery ~20 fixtures (checks, targets, narrate modes). **Deferred:** spell/item orchestrators; nightly real-model run with persisted scores. |
| ENG-7 | Tier-4 6-client sync-stress (P95 broadcast < 500ms) | roadmap §7 alpha gate | P6 | doc-only | Partial | **C3 harness shipped** (`services/ws-server/src/sync-stress/`): `runSyncStress` seeds the real `BattleRoom`, fans the production `writeProjection` Yjs update out to N client docs that each `readProjection`, and reports latency/size percentiles + a convergence check. A vitest gate asserts **P95 < 500ms** with 6 clients over ≥200 broadcasts, all converged. First recorded run (`docs/perf/sync-stress.md`): 6 clients · 300 broadcasts · p95 ≈ 1.1ms · ~1.2KB/update. **Deferred:** a real-WAN load test against a deployed Hocuspocus (network RTT, real clients), a richer action mix (moves/attacks/spells vs `end_turn` churn), and tracking p95 over time. |
| ENG-8 | Combat death/downed SRD-rule depth | M3 engine track (deferral-verify §14) | post-M5 / rules deepen | doc-only | Deferred | `combat/death.ts` always begins/continues death saves. Not modeled: **massive-damage instant death** (damage overflow ≥ max HP at 0 → dead, no saves); **crit / melee-within-5ft at 0 HP = 2 failures** (currently always 1; `DamageDealt` payload carries no crit flag); a **Stabilize action** (Medicine DC 10 / healer's-kit auto-stabilize — today stable only via 3 successes); **stable creature auto-regains 1 HP after 1 hour** (no encounter/world clock). |
| ENG-9 | Rest recovery + resource model depth | M3 engine track (deferral-verify §14) | E2+ / P5 | doc-only | Deferred | `short_rest`/`long_rest` restore **HP + exhaustion−1 only**; no spell-slot / class-resource recovery and **no hit-dice pool** tracking (caller passes `hitDice`+`dieSize` to `short_rest`). **Open design decision (rides here):** spell-slot source of truth — `spells.slots` is stored as explicit `{max,used}` today; the engine may later **own/derive `max`** from class/level (storing only `used`). Lock before the Spells tab does slot math. |
| ENG-10 | Reach-weapon support in OA **provoke** detection | M3 engine track (deferral-verify §14) | when OA provoke uses weapon reach | doc-only | Done | `EntityState.meleeReachFt` + `opportunityAttackReach()`; `handleMove` passes per-reactor reach into `provokesOpportunityAttack`; party seeding sets reach from equipped weapons (`meleeReachFromEquipment`); integration test in `engine.reactions.test.ts`. Attack/OA resolution already used weapon `rangeFt` (ENG-15). |
| ENG-11 | `end_encounter` command + teardown | M3 engine track (deferral-verify §14) | when encounter teardown is needed | doc-only | Done | **PR #192:** `EndEncounterCommand` + `EncounterEnded` projection clears `encounter` and strips action economy / reactions / readied on all combatants. Wired from `tutorialVictory` so Scene 5→6 leaves combat mode. |
| ENG-12 | Known/prepared spell-list enforcement on cast | P3 spell foundation (deferral-verify §18) | caster-config slice | doc-only | Done | **Shipped (#214):** `preparedSpellIds` on spellcasting state; `isSpellPrepared()` gate in `cast_spell` handler (`SPELL_NOT_PREPARED`); WS seeds prepared ids from character sheet on party load. |
| ENG-13 | Durable effect system (`ActiveEffect[]`) | P3 spell foundation (deferral-verify §19) | E2 effect system | doc-only | Partial | **Tracer shipped (#212):** `EntityState.effects[]`, `EffectApplied` events, `effectiveAc` + Bless attack bonus + Hunter's Mark on-hit damage; concentration-linked expiry; Shield expires start-of-turn. **Rung 2 (Jun 2026):** `attacks_against_disadvantage` (Blur); spell-attack modifiers + condition-on-save. **Rung 3 (Jun 2026, PR #245):** `speed_bonus` (Haste +30 ft); `remainingRounds` + `EffectsDurationTicked`; concentration-linked **condition cleanup** on `ConcentrationBroken`; `dispel_magic` command; registry `dispel-magic` cast routes to same handler. **Deferred:** save-roll stat modifiers, Dispel Magic CON save per effect, concentration save on all duration types, `EndConcentration` Live Play UI. |
| ENG-14 | Faction → combat `sides` seeding | M3 engine track (deferral-verify §14) | v1.5-ish enhancement | doc-only | Deferred | `areHostile` is team-id only; no bridge from Realms Faction relationships to encounter `sides`. Combat sides are deliberately distinct from Factions (charmed PCs, summons, NPC allies break a kind/faction heuristic); auto-seeding is an opt-in convenience layer. |
| ENG-15 | Weapon attack range enforcement in engine | M3 engine track / PLAY-6 | P4 | doc-only | Done | `AttackCommand.rangeFt` validated in `handleAttack` as `OUT_OF_RANGE` when both combatants are placed on a scene (defaults to 5 ft melee when omitted). Threaded through the client weapon picker, opportunity attacks, readied actions, and enemy AI melee. **Still deferred:** long/short range bands, cover on attacks, monster ranged loadouts (PLAY-15). |

---

## 5. Infra / setup (provision-when-needed)

| ID | Item | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|
| INFRA-1 | Trigger.dev `tr_prod_` runtime secret key | when app triggers tasks at runtime (P4 cascades) | doc-only | Partial | Runtime dispatch gated on `TRIGGER_SECRET_KEY` (`isTriggerConfigured`). **Documented in `.env.example`:** use `tr_prod_…` on Vercel for hosted async cascades/recaps/embed dispatch; nightly cron uses Trigger.dev deploy env only. **Still deferred:** actual Vercel env provisioning (Jordan action); Trigger.dev ↔ Vercel integration optional. |
| INFRA-2 | Vercel deploy | when needed | doc-only | Deferred | Repo pushed, unblocked. Pairs with the **Trigger.dev Vercel integration** (env-var sync Vercel ↔ Trigger.dev) once a hosted URL exists. |
| INFRA-3 | Sentry / PostHog account provisioning | P6 (alpha funnels) | doc-only | Deferred | Env-gated stubs already in code (e.g. `lib/observability/sentry.ts` is a no-op; `@sentry/nextjs` not installed). |
| INFRA-4 | Pricing rate lock + DM-allowance + AI-gen metering | before M8 closed beta | doc-only | Deferred | Commercial-from-closed-beta + 10 free DM chats already locked (`product-spec.md` §5). **Still open:** flat vs usage vs hybrid **rates**; the **paid-plan DM allowance model** after the free tier (unlimited vs monthly cap vs metered); and **whether AI generation counts against the 10-free-DM-chat meter** (`generation_events` audit gives cost observability, but the metering *policy* is undecided). |
| INFRA-5 | `gh` on PATH permanently (per-user) | done | doc-only | Done | `C:\Program Files\GitHub CLI` added to the user PATH (Jun 2026, via PowerShell `SetEnvironmentVariable`). `gh` resolves in new terminals; authed as `jordanlarch`. Note: PowerShell bulk `gh issue create` loops can silently no-op — use explicit sequential `& $gh` calls in issue-queue scripts. |
| INFRA-6 | Full normalized SRD ingest pipeline + scheduling (beyond nightly Open5e spell job) | P5–P7 | doc-only | Deferred | Migrate Open5e → custom SRD 5.2 ingest at GA (`data-sources.md` §1). |
| INFRA-7 | Auth / API-key cleanup (post-migration) | M3 #14 sync (deferral-verify §17) | post-migration cleanup | doc-only | Deferred | Two security-hygiene items from the Yjs sync work: **(1)** remove the ws-server legacy **HS256 JWT fallback** (`services/ws-server/src/auth.ts` tries JWKS ES256/RS256 then a legacy HS256 secret) once the Supabase signing-key migration finishes; **(2)** migrate the Supabase **legacy anon/service_role API keys** → publishable/secret keys (orthogonal to JWT signing; doesn't affect ws-server JWKS verification). |

---

## 6. Memory tier (P5 — RAG / embeddings)

The multi-tier memory architecture (`docs/data-sources.md` §6): pgvector embeddings,
retrieval, rolling session summary, and auto-recaps. Design settled in the 2026-06-22
grill; building as **tracer-bullet vertical slices**. The tracer spine (MEM-1/MEM-2)
proves embed → store → retrieve on Realms entities; the rows below it are the deferred
follow-ups that plug into the same seam.

| ID | Item | Source | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|---|
| MEM-1 | `@app/memory` package + pgvector store + `retrieveSimilar` primitive | data-sources §6 | P5 / M6 | #135 | Done | **Shipped (#137):** new `@app/memory` package — polymorphic `embeddings` table (migration `0015`, HNSW cosine) + `EmbeddingClient` seam (OpenAI `text-embedding-3-small` + deterministic feature-hashing fake) + card-chunk composition (contentHash, skip stubs) + `upsertSourceEmbeddings`/`retrieveSimilar`; PGlite+`vector` integration harness (21 tests). No app consumer yet (that's MEM-2). Refines §6's per-column sketch to a single polymorphic table. |
| MEM-2 | Embed Realms entities on write + reachable `memory.search` | data-sources §6 | P5 / M6 | #136 | Done | **Shipped (#138):** best-effort synchronous embed wired into realms create/update/generate/expandStub (`server/memory/embed.ts`; gated on `OPENAI_API_KEY`, never fails the mutation, structured cost logs); `memory.search` owner-scoped tRPC query over `retrieveSimilar`; backfill script (`npm run backfill:embeddings`). `@app/memory` gained `embedRealmEntity`/`embedRealmEntityBestEffort` and upsert now returns model+tokens. No `tr_prod_` dependency. Not yet wired into generation (GEN-4/MEM-6) or live turns (MEM-5). |
| MEM-3 | Rolling session summary (periodic condense of current session) | data-sources §6 | P5 | #143 | Done | **Shipped (#144):** `rolling_summaries` table (campaign-scoped upsert, migration 0016) + ws-server `session-summary.ts` (LLM `summarizeSession`, `shouldRegenerateSummary` cadence, best-effort `maybeUpdateRollingSummary`) regenerated **inline in ws-server** every `SUMMARY_EVERY` turns; injected into narration as a "story so far" block (`narrate()` `summary`). Resolves the open who-runs-it question → ws-server inline (no Trigger dependency). Env-gated on `ANTHROPIC_API_KEY`; offline-safe. Transient working memory — not an embedded RAG source (that's MEM-4). |
| MEM-4 | Sessions concept + auto-recap jobs (recap embedded as a source) | data-sources §6 / CAMP-6 / PLAY-12 | P5 | #145 | Done | **Shipped (#146):** `sessions` table (chat `[startSeq,endSeq)` span + recap + timestamps, migration 0017); `sessions.end` tRPC (records the unsessioned chat range → session row → recap) + `sessions.list`; `server/memory/recap.ts` (`generateRecap` LLM, `embedRecapBestEffort` as `session_recap` campaign-scoped, `runAndStoreRecap`); `generate-recap` Trigger task (durable route). Recap runs **inline best-effort** by default, dispatches to Trigger when `TRIGGER_SECRET_KEY` set (`tr_prod_`/INFRA-1 only for prod jobs). Offline-safe. Recaps reachable now via `memory.search`. **Since shipped:** CAMP-6 Sessions tab UI + the "End current session" button (#152, #214). **Still deferred:** live-turn recap injection (MEM-5 assembler). |
| MEM-5 | Live-turn RAG injection into the AI-GM prompt (5-part context assembler + rerank) | data-sources §6 | P5 | #139, #149 | Done | **Shipped (#140 + #150):** owner-scoped `retrieveSimilar` injected into the live AI-GM narration prompt as a background "world knowledge" block (`ws-server/world-knowledge.ts`; `narration.ts` `knowledge[]`; player-turn + check-outcome paths). **Tail (#150):** grown into the multi-category assembler — retrieves **lore** (`realm_entity`, owner-scoped) + **session recaps** (`session_recap`, campaign-scoped, MEM-4) and **reranks** (`cosine×weight + recencyBonus`, similarity floor, global top-k, recap provenance tag); `retrieveSimilar` now returns `createdAt` for recency. Rolling summary (MEM-3) stays injected separately as "story so far". Env-gated on `OPENAI_API_KEY`; offline-safe; lore-only back-compat preserved. **Pinned memory now slots in (MEM-8, #155); cross-link source type now slots in (GEN-5, #167)** — owner-scoped, weight 0.9, "Connection:" prefix. |
| MEM-6 | Generator RAG grounding (GEN-4) via `retrieveSimilar` | D11 / GEN-4 | P5 | #141 | Done | **Shipped (#142):** `loadRelatedLore` (`server/memory/related-lore.ts`) over `realm_entity` embeddings — owner-scoped, similarity-floored, self-excluding, best-effort — injected as an "existing entities in this world (for consistency)" block into the new/expand/regenerate prompts; `ownerId`(+`entityId`) threaded from `realms.generate`/`expandStub`/`regenerate`, the cascade Trigger task, and campaigns world-seed. Env-gated on `OPENAI_API_KEY`; offline-safe (no-op `[]` → prompts unchanged). First non-debug consumer of the retrieval seam. |
| MEM-7 | Nightly drift re-embed + async embed dispatch (Trigger.dev) | data-sources §6 | P5+ | #147 | Done | **Shipped (#148):** `@app/memory` `reembedRealmEntities` (contentHash-gated shared pass; `backfill:embeddings` reuses it); `embed-entity` runtime Trigger task; `reembed-entities` **scheduled** Trigger task (cron `0 9 * * *`, deployable without `tr_prod_`); `embedRealmEntityOnWrite` dispatches to `embed-entity` when `TRIGGER_SECRET_KEY` set, else synchronous best-effort embed (MEM-2 behavior unchanged). Async dispatch needs `tr_prod_` (INFRA-1) only in prod; nightly re-embed needs only `OPENAI_API_KEY`+`DATABASE_URL` in the Trigger env. Offline-safe. |
| MEM-8 | Pinned memory (durable DM-pinned facts in the live-turn rerank) | data-sources §6 / MEM-5 / CAMP-2 | P5 | #155 | Done | **Shipped (#155):** `pinned_memories` table (campaign-scoped, owner-set; migration 0018) + `pinned_memory` embedding source; `server/memory/pins.ts` (`embedPinBestEffort` / `deletePinEmbeddingsBestEffort`, best-effort + env-gated, `@app/memory` gained `deleteSourceEmbeddings`); `pins` tRPC router (`list`/`create`/`remove`, owner-scoped, embed on create / delete embeddings on remove); a "Pinned Memory" panel on the campaign Overview tab (closes the CAMP-2 pinned-memory widget). Offline-safe (pin recorded even with embeddings off; recoverable via re-embed). **Always-inject tuning shipped (#159):** pins moved out of the similarity rerank — the top-N (3) most-recent pins are now read directly from the table and injected into the GM prompt **every turn** (via `retrievePinnedMemories` + `loadCampaignPins`), not gated on `OPENAI_API_KEY`, so pins ground play even with the embedding tier off. **Still deferred:** similarity fallback for overflow pins beyond the always-inject top-N; a dedicated more-authoritative prompt block; pin-to-memory from Notes (CAMP-9) / end-session memory-pin step (PLAY-12) can reuse `pins.create`. |

---

## 7. Testing & technical debt

Recurring engineering gaps flagged across multiple sessions (deferral-verify §§11–23) that
weren't tracked as first-class rows. The deterministic engine (`@app/engine`) and `@app/db`
are well covered by unit/golden/PGlite tests; the gaps are above that layer.

| ID | Item | Source | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|---|
| TEST-1 | `apps/web` tRPC router + E2E test harness | recurring (deferral-verify §§12,19,20,21) | when a web test harness earns its keep | doc-only | Deferred | No router-level integration tests for `characters`/`smithy`/`realms`/`campaigns`/`pins`/`hooks`/`sessions`/`memory` and no browser/E2E smoke. Today only `@app/engine` + `@app/db` (PGlite) unit/golden tests and a few pure-helper unit tests under `apps/web/src/{lib,server/memory}` run in CI; auth-gated flows are manually verified. |
| TEST-2 | Real-Postgres CI (migrations + db tests) | P2 (deferral-verify §11) | post-GA / infra sprint | doc-only | Deferred | CI runs db tests against **PGlite** only; no `db:migrate` against a real Postgres/Supabase (or throwaway PG service) in GitHub Actions. PGlite golden tests deemed sufficient for event-store logic for now. |
| TEST-3 | Live-model / live-generation CI coverage | generator + ENG-6 (deferral-verify §§21,23) | P6 / ops | doc-only | Deferred | Generation paths (`generate`/`expandStub`/`regenerate`/`cascade`) and the ENG-6 adherence harness need `ANTHROPIC_API_KEY` / `OPENAI_API_KEY`; CI runs **fake-client** unit tests only. No scheduled nightly real-model run gates drift. |
| DEBT-1 | Dead engine tRPC endpoints | #14 sync (deferral-verify §17) | cleanup when confirmed unused | doc-only | Deferred | `engine.fixtureBattle` / `engine.simulateBattle` remain in `apps/web/src/server/trpc/routers/engine.ts` after the sandbox moved to the Yjs live session (`useLiveSession`); left in to avoid breaking router behavior. Remove once confirmed unused. |

---

## 8. v1.5+ / out-of-scope (linked, not restated)

These are **locked product cuts**, not backlog. Do not re-pitch without an explicit
decision update. Full lists:

- `docs/product-spec.md` §6 — consolidated v1 out-of-scope.
- `docs/00-consolidated-plan.md` — per-decision cuts.
- `AGENTS.md` "Considered And Rejected" — alternatives ruled out (human-DM tier, top-level
  Maps nav, lower combat tiers, collapsing the three libraries, LLM-does-math, etc.).

Quick index of the v1.5+ deferrals referenced by rows above:

| ID | Item | Source | Notes |
|---|---|---|---|
| ART-1 | AI image generation (tokens/portraits/maps on-demand) | Q16 | v1.5+. v1 = library + manual upload only. Referenced by REALM-7 auto-art, CAMP-10 art-style-lock. |
| V15-1 | STT (speech-to-text input) | Q19c | v1.5. TTS-only in v1 (see PLAY-10). |
| V15-2 | AI-styled / painterly map overlay | data-sources §AI-Styled Maps | v1.5+. Procedural geometry only in v1. |
| V15-3 | World Anvil import (Realms) | realms-library.md | v1.5. Referenced by REALM-8. |
| V15-4 | Spectator mode, highlight reel | live-play-surface.md | v1.5. |
| V15-5 | Deep-dive tutorials (Smithy/Realms/multiplayer) | roadmap §11 | v1.5+. |

---

## 10. SRD 5.1 → 5.2 audit

Full discovery matrix: **`docs/srd-version-audit.md`**. Re-run DB counts: `cd packages/db && npm run srd-audit:db`.

| ID | Item | Source | Deferred-to | Tracking | Status | Notes |
|---|---|---|---|---|---|---|
| SRD-AUDIT-0 | Discovery matrix + grep + prod DB document-key audit | handoff 2026-06-28 | — | doc-only | Done | **Locked decisions:** PDF (`SRD_CC_v5.2.1`) canonical; Open5e `srd-2024` ingest where PDF-aligned; **delete all characters** (no alias migration); spell handlers must match PDF; **5e-bits unused** (2014-only API — recommend doc removal). Prod: spells/monsters **319/325 × srd-2014**; items/bg/feats **2024**; species seed **12 × 5.1 subraces**; `SPELL_REGISTRY` **124** handlers. |
| SRD-AUDIT-1 | Rewrite `class-features.ts` (all 12 classes → 2024 SRD) | srd-version-audit.md | next slice | doc-only | Done | **All 12 classes** rewritten from `SRD_CC_v5.2.1.pdf`; **`subclassPickLevel`** fixed to L3 for Cleric/Druid/Sorcerer/Warlock/Wizard. Fighter Second Wind **2 uses**; Ranger **Favored Enemy / Deft Explorer** replaces 2014 stubs; no PHB subclass names in feature text. |
| SRD-AUDIT-2 | Spells ingest → `srd-2024` + prune + PDF/Open5e diff report | open5e-spells.ts | after AUDIT-1 or parallel | doc-only | **Done** | Flipped `OPEN5E_SRD_DOCUMENT_KEY` to `srd-2024`. Local/prod ingest: **339** upserted, **319** `srd-2014` pruned. Diff report deferred (names largely overlap). |
| SRD-AUDIT-3 | Creatures ingest → `srd-2024` + prune | open5e-creatures.ts | after AUDIT-2 | doc-only | **Done** | **331** upserted, **325** `srd-2014` pruned. Comments updated to SRD 5.2. |
| SRD-AUDIT-4 | Re-seed `srd-character-options.ts` — 9 PDF species, 2024 class copy | srd-version-audit.md | after AUDIT-1 | doc-only | **Done** | 9 unified SRD 5.2.1 species (Dragonborn, Dwarf, Elf, Gnome, Goliath, Halfling, Human, Orc, Tiefling); empty species ASI; prune legacy subrace slugs on seed; trait blurbs updated. |
| SRD-AUDIT-5 | Fighting style ↔ Codex Fighting Style feats; origin feat wiring | srd-version-audit.md | after AUDIT-4 | doc-only | **Done** | `FightingStylePicker` + sheet use Codex `featType=Fighting Style` (4 SRD styles); Human Versatile Origin picker on background step; background origin feat surfaced + persisted. |
| SRD-AUDIT-6 | `SPELL_REGISTRY` ↔ PDF canonical spell list (full ~360 handlers) | ENG-2/ENG-3 + audit | v1 | doc-only | **Done** | **339** Open5e catalog entries via `npm run generate:spell-registry`; **124** hand-authored combat overrides; golden tests on hand-authored only. |
| SRD-AUDIT-7 | Slot/multiclass engine vs 2024 SRD; remove EK/AT third-caster | spell-slots.ts, third-caster-slots.ts | after AUDIT-4 | doc-only | **Done** | Removed third-caster module; PHB→SRD UI copy on spells tab / level-up; pooled slots unchanged (SRD table). |
| SRD-AUDIT-8 | Delete all characters; drop `normalizeSubclassName` legacy aliases; fix fixtures | Jordan decision | with purge | doc-only | **Done** | `npm run purge:characters` removed **3** prod rows; dropped `SUBCLASS_NAME_ALIASES`; fixtures → Dwarf/Champion, Elf. |
| SRD-AUDIT-9 | Docs: `data-sources.md`, AGENTS.md — drop 5e-bits; state PDF-first policy | srd-version-audit.md | after ingest flips | doc-only | **Done** | Open5e-only ingest documented; 5e-bits in **Considered And Rejected**; consolidated plan + product-spec assumptions updated. |
| SRD-AUDIT-10 | Full SRD 5.2 PDF alignment — Codex IA, Gameplay Toolbox ingest, terminology | srd-version-audit.md | post–DATA-1b v1 | doc-only | **Done** | **Gameplay Toolbox** replaces Codex Advanced; `codex_toolbox_entries` (44 sample rows); nightly `codex_advanced_rules` ingest stopped; dead tRPC removed. **Follow-ups:** SRD-AUDIT-10-R1 (prod spell `srd-2014` regression), GRILL-EXPLORATION, GA drop of orphan `codex_advanced_rules`. See `docs/data-model-hazards.md`. |
| SRD-AUDIT-10-R1 | Prod spell doc_key regression — re-ingest `srd-2024` (319→339) | srd-version-audit.md | post–AUDIT-10 | ops | **Done** | **2026-06-27:** `npm run ingest:open5e` on prod — **339** upserted, **319** `srd-2014` pruned. Nightly Trigger job updated (SRD-AUDIT-10) to stop advanced-rules ingest. |
| SRD-AUDIT-12 | Runtime/UI congruence — no PHB / 2014 / 5.1 in code paths | srd-version-audit.md | post–AUDIT-10-R1 | doc-only | **Done** | Grep sweep apps + engine + LLM; fixed PHB comments + creation wizard label. See audit doc § SRD-AUDIT-12. |

---

## 12. SRD 5.2.1 feature-coverage (fidelity) — `SRD-FID-*`

Full coverage map: **`docs/srd-feature-coverage-audit.md`** (the SRD-shaped reference companion to the
version audit in §10). That doc answers *"does the engine implement the rule, correctly?"*; the rows
here are the **work-of-record** for the gaps. First audited 2026-06-29 against `SRD_CC_v5.2.1.pdf`.
**Keep §12 and the audit doc in sync** on every fidelity ship (see `.cursor/skills/ship-it/SKILL.md` §7).

**2026-06-29 fix pass (PR-pending):** all incongruences were **re-verified against the 5.2.1 PDF text**
before touching code. FID-1/3/4/5/6 fixed; FID-12 partially fixed (Flame Strike); **FID-10 retracted**
(the audit claim was the 2014 spell — 2024 Sleep is a Wisdom save, so the engine was already a valid
tracer; the 5-ft-Sphere/Concentration gaps were fixed anyway). Engine suite green (603 tests) + typecheck.

### 12.1 Incongruences — implemented but contradicts the PDF (correctness bugs)

| ID | Item | Tracking | Status | Notes |
|---|---|---|---|---|
| SRD-FID-1 | Exhaustion uses 2014 tiered model, not 2024 uniform (−2/level D20, −5ft/level, death at 6) | #310 | **Done** | `combat/conditions.ts` (`exhaustionD20Penalty`, `effectiveSpeed` −5/level) + penalty threaded into attack/check/save/spell-attack handlers. PDF p.181. |
| SRD-FID-2 | Background ability bonuses (+2/+1) never applied; species ASI empty; UI copy contradicts behavior | #311 | **Done** | PR #326 — Abilities step: `BackgroundAsiPicker` (+2/+1 or three +1s); `applyBackgroundAsi` in `finalScores` → saved on create. Engine helpers in `character-build.ts`. PDF p.83. |
| SRD-FID-3 | Frightened missing can't-approach / ability-check disadvantage | #312 | **Done** | `combat/conditions.ts` (`checkMode`, `frightenedSources`) + can't-approach in `handleMoveEntity`. LOS gate kept as documented always-on approximation. PDF p.182. |
| SRD-FID-4 | Faerie Fire modeled as sphere; should be 20-ft cube | #313 | **Done** | spell registry → `cube`. |
| SRD-FID-5 | Spirit Guardians sphere; should be 15-ft emanation (no Emanation shape in engine) | #314 | **Done** | Added `emanation` AoE shape (taxonomy + cast resolution, caster-centered, excludes caster). |
| SRD-FID-6 | Open5e converter defaults every save spell to `half_damage` (~165 spells wrong) | #315 | **Done** | `open5e-spell.ts` infers `no_effect` unless text says "half"; **registry regenerated — 68 spells corrected** (e.g. Sacred Flame, Hold Person). |
| SRD-FID-7 | Hex reuses Hunter's Mark machinery (wrong damage type, weapon-only) | #316 | **Done** | Dedicated `hex` effect modifier: necrotic bonus on any attack by caster, `hexAbility` on cast, check disadvantage via `hexCheckDisadvantage`. |
| SRD-FID-8 | Scorching Ray auto-hits; should be separate ranged spell attacks | #317 | **Done** | `SpellProjectiles.spellAttack`; per-ray `resolveSpellAttackRay` + `AttackResolved` per dart. Magic Missile unchanged (auto-hit). |
| SRD-FID-9 | Revivify heals a living ally; should raise the recently dead | #318 | **Done** | PR #347 — `CreatureRevived` path; dead-only target validation; raises to 1 HP (clears `dead`/`deathSaves`). |
| SRD-FID-10 | ~~Sleep flat save; should be 5d8 HP-total budget~~ — **RETRACTED** (2014 claim) | #319 | **Done/retracted** | 2024 Sleep *is* a Wisdom save → engine was already a valid tracer. Fixed remaining gaps: 5-ft Sphere + Concentration. |
| SRD-FID-11 | Counterspell / Dispel Magic lack interrupt + spell-level contest | #320 | **Done** | PR #346 — Counterspell level contest via `counteredSpellSlotLevel`; Dispel removes all ≤3rd-level effects + ability check per 4th+; cast staging (`PendingSpellCastStaged`) + `pass_counterspell` + Counterspell resolves/cancels pending cast; Live Play reaction prompt; ws-server pauses enemy AI during window. PR #349 — sandbox seeds **Aldric** (Counterspell) + **Cult Fanatic** foe for prod verify. |
| SRD-FID-12 | Tracer spells simplified | #321 | **Partial** | **Flame Strike done** (cylinder + 5d6 fire/5d6 radiant). PR #348 — Wall of Fire / Polymorph / Spiritual Weapon. PR #352 — **Moonbeam** persistent zone + turn-start Con save tick; **Call Lightning** storm cloud zone + `strike_call_lightning` action strikes + Live Play strike button; **Spirit Guardians** caster aura + hostile turn-start Wis save tick. PR #355 — **Cloudkill** / **Stinking Cloud** zones + **Haste** extra action. PR #356 — **Banishment** (save, remove from battlefield, return on concentration end). Remaining (Slow, Darkness, …) overlap ENG-3. |
| SRD-FID-13 | Damage at 0 HP: no crit=2 failures, no instant death | #322 | **Done** | `DamageDealt.critical` flag; projection instant-death overflow (SRD p.17); crit doubles death-save failures at 0 HP. |

### 12.2 Next-up — cheap, high-visibility combat completeness

| ID | Item | Tracking | Status | Notes |
|---|---|---|---|---|
| SRD-FID-14 | Standard actions Dash / Disengage / Dodge / Help / Hide (only Attack/Magic/Ready exist) | #323 | **Done** | PR #327 — `standard-action-handlers.ts` + OA/dodge/save integration. PDF pp.180–183. |
| SRD-FID-15 | Cover (half +2 / three-quarters +5 / total) for AC + Dex saves | #324 | **Done** | PR #327 — `combat/cover.ts`; weapon attacks + Dex saves + Sacred Flame `ignoreCover`. Total cover = wall-only LOS block. |

### 12.3 Partial / missing fidelity gaps (depth backlog)

Tracked here; several alias to existing engine rows (don't mint duplicates — fix under the linked ID).

| ID | Item | Tracking | Status | Notes |
|---|---|---|---|---|
| SRD-FID-16 | Saving-throw + skill/tool/weapon **proficiency** on `EntityState` | doc-only | **Done** | PR #328 (saves), #347 (skills), **#350** (`toolProficiencies` + `weaponProficiencies` on `EntityState`; trap detect/disable + Live Play loadout; ws-server sheet meta). |
| SRD-FID-17 | Surprise (combat order) | doc-only | **Done** | PR #329 — `resolve_surprise` command; surprised flag blocks move/actions/reactions on first turn. |
| SRD-FID-18 | Difficult terrain movement cost | doc-only | **Done** | PR #330 — `SceneMap.difficultCells`; path-based `movementCostFeet` doubles entered squares. |
| SRD-FID-19 | Resistance / Vulnerability / Immunity + temp-HP **grant** command | doc-only | **Done** | PR #331 — `adjustDamageAmount`; entity damage lists; `grant_temp_hp` + False Life temp HP. |
| SRD-FID-20 | Condition depth (grapple escape, frightened gates, blinded/deafened auto-fail, petrified resistances) | doc-only | **Done** | PR #333 — `escape_grapple`; frightened LOS gates; blinded/deafened auto-fail; petrified resist in `adjustDamageAmount`. |
| SRD-FID-21 | Class + subclass feature **mechanics** (base-class signatures wired; subclasses → FID-21b) | doc-only | Partial | PR #334–338, #346, #347, **#351 Live Play** (sandbox F9 Thorin, Paladin Galen, Cleric Mara; Spiritual Weapon strike; Lay on Hands picker; Indomitable prompt). Remaining class features still deferred. **Note:** one Metamagic option per cast (combos deferred). |
| SRD-FID-21b | Subclass feature **mechanics** (all 12 SRD subclasses) | doc-only | **Done** | PR #339 — Life Domain *Disciple of Life*. PR #340 — Champion, Berserker, College of Lore, Circle of the Land. PR #341 — Hunter *Colossus Slayer*, Evoker *Potent Cantrip*, Fiend Patron *Dark One's Blessing*. PR #342 — Thief *Fast Hands*, Oath of Devotion *Sacred Weapon* (subclass-gated CD), Draconic Sorcery *Draconic Resilience*, Open Hand *Flurry + Open Hand Technique*. **Partial depth remains** on higher-level subclass features and Life Domain remainder (Preserve Life CD, domain spells, Blessed Healer). |
| SRD-FID-22 | Species trait mechanics (Darkvision, Breath Weapon, Lucky, lineages…) — 0 wired | doc-only | Missing | |
| SRD-FID-23 | Feat mechanical coverage (8 wired of full Codex set) | doc-only | Partial | |
| SRD-FID-24 | Weapon properties (Light/Heavy/Loading/Ammo/Thrown/Versatile/long-range) + **weapon-mastery effects** | doc-only | Partial | mastery is display-only; see ENG-15 for range bands. |
| SRD-FID-25 | Armor: stealth disadvantage, Str requirement, **Armor Training** (2024), don/doff | doc-only | Partial | |
| SRD-FID-26 | Tools, mounts & vehicles, lifestyle, hirelings, services | doc-only | Missing | |
| SRD-FID-27 | Spellcasting rules: ritual casting, V/S/M components, prepared-vs-known class models, runtime Warlock/multiclass slots | doc-only | Partial | |
| SRD-FID-28 | 2024 glossary mechanics: Bloodied, Heroic Inspiration, Emanation/Cylinder AoE, dehydration/malnutrition/suffocation | doc-only | Missing | Emanation also needed by SRD-FID-5. |
| SRD-FID-29 | Magic-item effect schema, **attunement-limit enforcement** (max 3), charges/activation, cursed-item type | doc-only | Missing | catalog-only today; see DATA-1d. |
| SRD-FID-30 | Mounted combat, Underwater combat | doc-only | Missing | |
| SRD-FID-31 | Crafting (nonmagical, potions of healing math, spell-scroll casting, magic-item crafting) | doc-only | Missing | |
| SRD-FID-32 | Advanced spell classes: summoning, polymorph/wild-shape stat swap, persistent walls/zones, teleportation, divination, banish/restore/command-obedience | doc-only | Missing | overlaps ENG-3. |

> **Out of SRD scope (no `SRD-FID`):** AI-GM chat, Realms generators, Smithy, Quests, overworld map/discovery,
> memory/recap tier, Yjs multiplayer, tutorial, GM persona / art-style / play tempo — these are the product
> layer on top of the ruleset and are governed by their own families above (GEN-*/REALM-*/SMITH-*/CAMP-*/PLAY-*/MEM-*/TUT-*).

---

## 11. Maintenance

- When you ship an item, set its **Status** to `Done` (and add the merge commit/PR in
  Notes) rather than deleting the row.
- When you add a new deferral anywhere (a code TODO you're punting, a scoped-out feature),
  add it here first; that's the rule.
- To mirror a batch to GitHub issues, run the `to-issues` skill against the relevant
  section (only if Jordan wants GitHub mirroring for that batch).
