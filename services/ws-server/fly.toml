# Fly.io deploy skeleton — wire when ws-server is implemented (P2).
app = "loreforge-ws"
primary_region = "iad"

[http_service]
  internal_port = 1234
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 0
